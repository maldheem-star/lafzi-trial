# مخرجات تشغيل الاختبارات محلّياً — تُكتب عند كل `sh tests/run.sh` ولا تُحفظ
.suite.log

# مجلّد التجريب يُنشأ في النشر (من فرع التطوير) وفي اختبار العزل محلّياً —
# ولا يُلتزَم أبداً: وجودُه في المستودع يُصادم خطوة الـcheckout في deploy-pages.yml
/preview/
