# مشروع هيا — دليل العمل

هذا الملفّ ذاكرةُ المشروع. اقرأه قبل أي تعديل. مكتوب بالعربية لأن صاحب المشروع
يعمل بها، والمخاطَبة في التطبيق كلّها عربية.

## ما هو

تطبيق تعليمي في **ملفّ واحد** (`index.html`، ~٥٩٠٠ سطر، بلا خطوة بناء) لثلاثة أبناء:

| مَن | الرابط | ماذا يرى |
|---|---|---|
| هيا (١١ سنة، سادس ابتدائي، A1) | `index.html` | كل الأقسام: موهبة/قدرات، الإنجليزية، الأساسيات، التقوية، المحادثة |
| محمد (١٩ سنة، B1) | `mohammed.html` | المحادثة، فهم الاستماع، فهم المقروء، الكتابة، لعبة الاستماع |
| إلياس (١٨ سنة، A2) | `elias.html` | المحادثة، فهم الاستماع، فهم المقروء، الكتابة، لعبة الاستماع |

**العمر والمستوى في `PROFILES` لا في الشيفرة** — كان النموذج يُخاطب الجميع بعمر
إحدى عشرة سنة، فيُعرض على محمد وهو يتحدّث عن الجامعة: «I want new shoes». والعمر والمستوى حقلان
منفصلان عمداً: إلياس ابن ثمانية عشر ومستواه A2 — رجلٌ في عمره، مبتدئ في إنتاجه.

**والمستوى يُقاس لا يُفترَض.** كان الثلاثة A2/B1/A2 بالحدس. وقياسٌ حقيقي (١٥ أغسطس)
على المعيار الدولي CEFR — إنتاجها المنطوق الفعلي مقابل وصف كل درجة — نزّل هيا إلى
**A1**: شظايا ونسخٌ حرفيّ («I want beach.»، «I eat a sandwich.» بحاضر بدل ماضٍ)،
ودقّة قواعدها على تراكيب فوق الأساسي (المبني للمجهول، الشرط، الموصول) **٣٥٪**
(٦٦/١٨٩). ومحمد B1 تأكّد: يسرد رحلةً بجملٍ مترابطة ويحاول جملة شرطية معقّدة رغم
أخطائها. وإلياس A2 تأكّد: جملٌ قصيرة سليمة، وينهار حين يحاول التركيب.

الهويّة **من الرابط وحده ولا تُحفَظ** — لئلّا يجرّ آخرُ من فتح الجهاز التاليَ إلى
ملفّه. وصفحتا الأخوين تُحوَّل نصوصهما إلى خطاب المذكّر في العرض (`MASC_W` +
`genderizeInit`)، وصفحة هيا لا يُنصَّب فيها المراقب أصلاً.

**النشر:** GitHub Pages من `main` تلقائياً — `https://maldheem-star.github.io/lafzi-trial/`

## البروتوكول الملزِم — يُنفَّذ بلا تذكير (٢٥ أغسطس)

### ٠) لا تخترع: ابحث عن المعيار العالمي أوّلاً
عند أي طلب (شيفرة، توليد محتوى، تصميم، خوارزمية، **مقياس**): ابحث عن المعيار المنشور
المُراجَع المستخدَم عالمياً، وطبّقه كما هو، **ووثّق اسمه ومصدره وتاريخه هنا**. وإن لم
تجد معياراً فقل **«لا يوجد معيار واضح»** واسأل قبل أن تبني. وإن تعارض المعيار مع حالتنا
فناقِش قبل التعديل. **ممنوع اختراع**: خوارزميات، مقاييس، تنسيقات، أسماء، بنى بيانات.

**المعتمَد فعلاً في هذا المشروع** (لا يُستبدَل بغيره بلا نقاش):

| الغرض | المعيار | المصدر |
|---|---|---|
| التعلّم المتباعد | **FSRS-6** | مُقابَل بـ`ts-fsrs@5.4.1` في `test_fsrs.js` |
| تشابه النصوص | **w-shingles (k=3)** | Broder 1997 |
| معايير العبور | **BKT** | Corbett & Anderson 1994 |
| صعوبة المحتوى | **CEFR / English Profile** | كامبريدج |
| صيغة الاختبار | **Cambridge YLE/KET/PET، TOEFL Junior** | نصوص كامبريدج |
| الحكم النحوي | **Grammaticality Judgment Task** | Spinner & Gass |
| تصديق التصحيح | **LanguageTool** حَكَماً | مدقّقٌ قائم على قواعد |
| النطق | **Azure phoneme assessment** + speechocean762 مسطرةً | — |
| نسبة النجاح المثلى | **قاعدة الـ٨٥٪** | Wilson et al. 2019؛ Bjork |
| ثراء المفردات | **LFP** | Laufer & Nation 1995 |
| تعليم الكتابة | **SRSD، Writing Next، دورة سيدني** | Graham & Perin 2007؛ Rothery & Martin |
| أخطاء الحساب | **BUGGY/DEBUGGY، Repair Theory** | Brown & Burton 1978؛ VanLehn |
| اتّجاه النصّ | **UAX #9** | Unicode |

**وSM-2 ليست ترقيةً بل تراجع**: FSRS-6 خلفُها المُعايَر على مليارات المراجعات، وهو
المعتمَد هنا ومُثبَتٌ باختبار. لا يُستبدَل بـSM-2/SM-18.

### ١) القرارات الثابتة — لا تُراجَع
- الأسئلة الإنجليزية **للجميع** — قرارٌ نهائي.
- **أي طلبٍ غامض: توقّف واسأل قبل التنفيذ.**
- **«محمد» = محمد وحده · «الجميع» = الثلاثة · بلا اسم = اسأل.**
- لا تبنِ من الصفر ما يمكن تعديله.

### ٢) قبل أي تعديل
اقرأ الملفّات المتأثّرة كاملة · ابحث عن **كل** المواضع المشابهة لا موضع الطلب وحده ·
حدّد الاختبارات المناسبة · ابحث عن المعيار العالمي · واسأل فوراً إن كان الطلب غامضاً.

### ٣) بعد أي تعديل — تلقائياً
شغّل الاختبارات · **التقط لقطات شاشة للمواضع المتأثّرة** · افحص RTL/LTR في كل عنصرٍ
يخلط العربية بأرقامٍ أو إنجليزية · ابحث عن نظيرٍ نسيتَه · وثّق الاكتشاف والمعيار هنا.

### ٤) قاعدة العرض البصري
أي تعديلٍ يمسّ التنسيق أو الاتّجاه ⇒ **لقطة شاشة إلزامية**. ولا يُكتفى بفحص DOM:
**الاتّجاه قد يكون صحيحاً نصّياً ومقلوباً بصرياً** — وقع فعلاً أربع مرّات في قسم الإحصاء.

### ٥) قاعدة القياس
لا تدّعِ نجاحاً بلا رقم · «الأسئلة متكرّرة» ⇒ قِس التكرار **لكل قسمٍ على حدة** ·
كل ادّعاءٍ يحتاج دليلاً قابلاً للقياس.

### ٦) قاعدة الإصلاح الشامل
أي خطأ تُصلحه: ابحث عن نظائره في كل الملفّات، **ووثّق كم موضعاً فحصتَ وكم أصلحتَ**.

### ٧) قاعدة الذاكرة
كل جلسة تبدأ من الصفر، وهذا الملفّ هو الذاكرة الوحيدة. أي قرارٍ أو اكتشافٍ أو خطأ
يُوثَّق **فوراً**. ولا تُعلَّق آليةٌ على أن يتذكّر أحدٌ خطوةً يدوية (درس ١٨ أغسطس).

### ٨) قاعدة الفحص النقدي
بعد أي بناء افحصه نقدياً قبل اعتماده: تكرارٌ خفيّ، حالات حافّة، افتراضاتٌ خاطئة.
**واستعمل مقاييس معيارية ولا تخترع مقياساً** (درس ٢٢ أغسطس: مقياسي المصنوع باليد
أطلق بلاغاتٍ كاذبة، فرُقِّع بدل أن يُستبدَل).

### ٩) قواعد تقنية ثابتة
- قسمٌ يخلط العربية بالرياضيات/الإنجليزية ⇒ لقطة شاشة قبل الشحن.
- لا مطابقة حرفية للّهجات: `en-US` ≠ `en_US` — وحّد الفاصل قبل المقارنة.
- لا تعتمد على صوتٍ أو خطٍّ بعينه من بيئة التطوير.
- الحسابات المعقّدة ⇒ سكربت `node` مستقلّ للتحقّق، لا حسابٌ ذهني.
- **ادفع لكل التزامٍ فور اكتماله** — نسخةٌ احتياطية لا نشر.

## قرارات صاحب المشروع — ثابتة، لا تُراجَع

1. **المفاتيح أسرار.** لا يُطلب مفتاح في المحادثة ولا يُلصق فيها أبداً. هو يضعها
   بنفسه في أسرار Supabase. والتشخيص يذكر **اسم** السرّ وطوله وبصمته فقط.
2. **لا دفع.** لا اشتراك ولا شحن رصيد. الطبقات المجانية وحدها (Groq الآن).
3. **لا بناء علاج من الصفر لكل مهارة.** الحلّ العامّ مقدَّم على الحلّ المفصَّل.
4. **التعب والسهر ليسا تفسيراً** لضعف نتيجة. ابحث عن سبب في البيانات أو قل لا أعرف.
5. **التطوير على فرع `claude/talent-app-fixes-visuals-poevc6`**، ثم دمج إلى `main`،
   ثم دفع الاثنين.
6. **الاختبار قبل الشحن** بخادم محلّي وPlaywright — لا شحن بلا تشغيل.
7. **الصراحة على التجميل.** إن لم تعرف السبب فقل ذلك، ولا تخترع تفسيراً.
8. **لا تكرار في أيّ قسمٍ لأيٍّ من الثلاثة — بنكٌ ثابت وحده لا يكفي، ولا مسؤوليتي تنتهي
   بالتوليد.** كل قسم (استماع/مقروء/كتابة/قواعد/STEP/أزواج الاستماع/فيديو، ولاحقاً أي
   قسمٍ جديد) يجب أن ينمو بتوليدٍ آليّ للأسئلة والمواضيع والشروح لا ببنكٍ مؤلَّفٍ يُعاد
   تدويره — درس ٢٠ أغسطس (تحت) أثبت أن غياب التوليد عن قسمٍ واحد (`gram`) أنتج ٨٥٪
   تكراراً حرفياً. **وصحّة كل سؤالٍ مولَّد ومطابقته لأهدافنا التعليمية مسؤوليتي أراقبها
   لا أفترضها** — تحقّقٌ لغوي (لا تلوّث لغة أخرى، الصواب فعلاً داخل الخيارات)، ومطابقة
   المستوى (i+1 أو مطابقة تامة بحسب القسم)، ومطابقة الصيغة المعياريّة (Cambridge/English
   Profile بحسب القسم) — لا توليدٌ يُشحَن بلا هذا التحقّق، كما يفعل `parseGenBlock` أصلاً
   لقسمَي الاستماع/المقروء.

## طريقة العمل المعتادة

يسأل: «كيف اختبارات هيا؟» ⇒ تُستعلَم Supabase ⇒ تقرير بالأدلّة (أرقام وأمثلة من
كلامها) ⇒ اقتراح علاج مرتّب ⇒ يقول «نفذ» ⇒ تُنفَّذ وتُختبر وتُدفَع.

**التقرير يجب أن يميّز:** ما تثبته البيانات، وما هو استنتاج، وما لا يُعرف. وقد
حدث أكثر من مرّة أن كذّب الفحصُ تفسيراً بدا بديهياً — فافحص قبل أن تقول.

## القاعدة الثالثة تنطبق عليّ أنا — درس ١٤ أغسطس

قال صاحب المشروع: **«أنت تبني من الصفر وتعبتني»**. وكان محقّاً: بوّابة القسمة، وبوّابة π،
وبطاقة التقابل، وبوّابة الفجوات، وبوّابة الاستلاف، وبنك الكلمات، وبوّابة الصدى — كلّها
بيدي. وأسوأها أن **ثلاث آليات «اكتشفتُها» في الطرح منشورةٌ مسمّاةٌ منذ ١٩٧٨**
(Brown & Burton، BUGGY/DEBUGGY، ثم Repair Theory عند VanLehn: فهرسٌ لأكثر من مئة
خطأ منهجيّ مستخرَجٍ من آلاف الأطفال).

**فقبل بناء أي علاج: ابحث عن اسمه في الأدبيات.** والمُعتمَد الآن:

| المكوّن | البديل المعياري | الحالة |
|---|---|---|
| استنباط الآليات في كل جلسة | `MATH_BUGS` — فهرس الأخطاء المسمّاة | ✅ |
| سلّم التكرار الثابت | FSRS-6 | ✅ |
| تصديق تصحيح النموذج | LanguageTool حَكَماً | ✅ |
| التدريب المكتّل | التشابك (`BASICS_MIX`) | ✅ |
| قياس الدقّة وحدها | معدّل الطلاقة DCPM | ✅ |
| سلّم الإتقان بأرقام اخترتُها (`BASICS_REQ=2`…) | BKT/PFA (`pyBKT`) | لم يُنفَّذ |
| استعلاماتي اليدوية | صيغة DataShop + منحنى التعلّم | لم يُنفَّذ |

**وما نفعله وهو أصلاً أفضل ممارسة** (فلا يُهدَم): السلّم المتلاشي (worked examples)،
والنزول عند التعثّر (expertise reversal)، والشرح مع كل تصحيح (elaborated feedback)،
وسؤال «لماذا اخترتِ؟» (self-explanation)، وخطّ الأعداد والمصفوفة تمثيلين معياريين.

**وتنبيه على موهبة نفسها**: الأدلّة على أن تدريب القدرات العامّة ينتقل خارج التدريب
**ضعيفة**. وما ينفع: التدرّب على شكل الاختبار، وإتقان الأساس — والطرح والقسمة أساسٌ حقيقي.

### مكتبة الأخطاء: الخطأ إجراءٌ يُحاكى لا فرقٌ يُطابَق

`bugOf(op,a,b,given)` يُنفّذ كل إجراء مكسور ويقارن ناتجه بجوابها. وهذا يفصل ما لا يفصله
الفرق: ٨١−٧٣ إجابةً ١٨ هي `borrow_no_decrement`، وإجابةً ١٢ هي `smaller_from_larger` —
وفرقاهما (+١٠ و+٤) لا يقولان أيّ إجراءٍ جرى.

**والبوّابة تُعلَّق على اسم** في جدول `BUG_GATES`، فإضافة بوّابة صارت سطراً لا وضعاً يُبنى.
والاسم يدخل السجلّ (`«١٨ · الصواب ٨ · فرق +١٠ · borrow_no_decrement»`) فيُجمَّع بالاستعلام.

**ووُصلت بوّابتان — درس ١٥ أغسطس**: `quot_off_one`/`quot_off_one_up` تكرّرا ١٣ مرّة عبر
يومين (أكثر من أيّ خطأ آخر)، وثلاثٌ من خمس وقعت في المرحلتين ٢-٣ حيث `basicsDivGated`
لا يعمل (يبدأ من المرحلة ٤) — فأُضيف سطرٌ في `BUG_GATES` يُسلِّح مصفوفة القسمة الموجودة
أصلاً عند تكرار الاسم، لا أداة جديدة. وبوّابتا الفجوات والاستلاف (`ob1`/`bt`) كانتا تعملان
في الأساسيات وحده رغم أن `count_all_not_gaps` تكرّر في `fact_fluency` ٤ مرّات بلا علاج —
وُصلتا إلى هناك بالآلية نفسها. **وكشف الوصل خطأً كامناً**: البوّابة كانت تُسلَّح بطرفي
عائلة الضرب (`fam.a/fam.b`) لا بطرفي السؤال المعروض في القسمة (٣٦÷٤ قد تُبنى من عائلة
٤×٩) — أُصلح إلى `basicsOperands()` قبل أن يُبنى عليه شيء.

## المبدأ الهندسي الذي أثبت نفسه

**البوّابة البنيوية تسبق النصيحة.** المنع أنفع من التنبيه، والأرقام تُثبت ذلك:

- بوّابة مصفوفة القسمة: ٣٨٪ ⇒ ٩٣٪
- بوّابة الفهم المقروء: ١/٣ ⇒ ٣/٣
- بوّابة المنطق: ٠/٣ ⇒ ٣/٣
- بوّابة التسرّع في التقوية: أنهت الإجابات تحت ثانيتين تماماً (وكشفت أن الخطأ
  لم يكن في السرعة — وهذا نفعٌ آخر: البوّابة تُثبت أو تنفي التفسير)

وكل بوّابة **مشروطة** ما أمكن: تُسلَّح عند العَرَض وتنحلّ عند زواله، فلا يُعاقَب
من لا يحتاجها.

### شكل الخطأ لا وقوعه — درس ١٣ أغسطس

خمس عشرة إجابة في الطرح، **صفر صواب**، وهي تختار من أربعة (التخمين وحده يعطي ٢٥٪).
والسبب أن أخطاءها منهجية والمموّهات `mcNum(ans,[ans+1,ans-1,ans+10])` — فخطؤها يقع
على مموّهٍ معروض في كل مرّة. ثلاث آليات، ولا خطأ واحد عشوائي:

| الآلية | العدد | الشكل |
|---|---|---|
| عدّت الأعداد لا الفجوات | ٧ | الصواب **+١** بالضبط |
| استلفت ولم تُنقص العشرات | ٤ | الصواب **+١٠** |
| أسقطت آحاد المطروح منه | ٤ | الصواب **−١**، وكلّها ينتهي بـ١ |

**والخطأ يعبر الأقسام**: النرد (٧−٥) ثلاث مرّات كلّها ±١، وفيبوناتشي مرّتان،
والوسيط ٦٥÷٥. فسؤال النرد **طرحٌ متنكّر** — وفرضيّتي القديمة أنه «صدى الرقم»
كذّبتها البيانات.

**فالعلاج على الشكل لا على الوقوع**: `basicsErrDelta()` يقيس الفرق، و`shapeRecord`
يُسلّح البوّابة بتكراره لا بأوّل زلّة (`SHAPE_ARM=2`) ويحلّها بالصواب:
- **`ob1*`** لعائلة `+١`: تقفز على خطّ الأعداد بيدها فترى الفجوات.
- **`bt*`** لعائلة `+١٠`: تُنقص خانة العشرات بيدها قبل أن تنتقل.
- وعائلة `−١` بلا بوّابة بعد — يُحكم عليها بعد أثر الاثنتين.

**و`BASICS_BACK`**: الأساسيات لم يكن فيها نزولٌ إطلاقاً (`basicsStage++` وحدها).
فجلست خمس عشرة مرّة على الدرجة نفسها بلا مثالٍ محلول. **سلّمٌ يصعد ولا يهبط ليس
سلّماً** — والإملاء عنده النزول منذ شهر. والبوّابة تسبق النزول: العلاج ثم الرجوع.

## البنية

- `index.html` — التطبيق كلّه. كتلتا `<script>`، توجيه بـ`mode` عبر `render()`.
- `supabase-functions-tutor.ts` — المعلّم/شريك المحادثة (Groq، ثلاثة أوضاع: شرح
  ومحادثة ومراجعة). المراجعة وحدها تمرّ على حَكَم LanguageTool.
- `supabase-functions-assess-azure.ts` — تقييم النطق بالفونيمات.
- `supabase-functions-assess-pronunciation-groq.ts` — تفريغ الصوت (Whisper).
- `tests/` — ٣٩ مجموعة اختبار. `sh tests/run.sh` بعد تشغيل خادم على ٨٩٣١.
  و`tests/pron_bench.js` خارجها: يحتاج شبكةً ومجموعة speechocean762، فيُشغَّل يدوياً.

### فهم الاستماع — ينفع الثلاثة سواءً

الإملاء يقيس التفريغ الحرفي، والمحادثة تقيس الإنتاج، وبينهما فجوة: أن تُفهَم معنى ما
يُقال بلا كتابته حرفاً حرفاً أو إنتاجه بنفسها. لا شيء كان يقيس هذا.

**الصيغة معياريّة لا مخترعة**: Cambridge (YLE/KET/PET) وTOEFL Junior — مقطعٌ قصير
يُسمع، فسؤال اختيار من متعدّد عن معناه، **والسؤال معروض قبل الاستماع** (يُصغى
بانتقائية، لا كبوّابة بعده). ومرّتا استماعٍ معياريّتان للمبتدئ (A1/A2، تخفّف الحمل
المعرفي) ومرّةٌ واحدة لِمن فوقه (B1، كما IELTS) — إرشادٌ نصّي لا قفلٌ تقني، فزرّ
🔊 لا يُمنع: لا فائدة تشخيصية من منعه، ويُسجَّل عدده فحسب.

- **`LISTEN_BANK`** مؤلَّفٌ بالضرورة (لا بنك مسموعات مفتوح الرخصة يصلح: VOA عامّ
  النطاق لموادّه الأصلية وحدها، وBBC محظور إعادة توزيعه، وTatoeba تراخيصها لكل
  تسجيلٍ على حدة وغالباً جملة واحدة لا حواراً) — كما يؤلّف كل مصدرٍ معياريّ محتواه.
  **والتباعد `fsrsUpdate` نفسها** — لا خوارزمية جديدة، مفتاحٌ محلّي فقط.
- كل متعلّم يرى **بنك مستواه هو بالضبط** (`listenBankFor`، مطابقة لا "≤")، فمحمد B1
  يرى فقرات سردية، وهيا A1 ترى حقيقةً واحدة ملموسة في جملة.
- **الحكم عليه**: دقّة كل مستوى على حدة (`domain='listen' and qtype='A1'`…)، ونسبة
  استعمال زرّ الإعادة — إن كثر التكرار عند B1 رغم إرشاد «مرّة واحدة» فالمستوى المقاس
  له قد يكون أعلى من إنتاجه الفعلي (فهمٌ يسبق إنتاجاً، وهذا معروفٌ في اكتساب اللغة).
- **بنكٌ ثابتٌ (١٠/مستوى) يُستنفَد بجلسةٍ أو جلستين — درس ١٥ أغسطس.** طلب صاحب
  المشروع من هيا التمرّن فظهرت «لا شيء مستحقّ اليوم»: `buildListenPlan` كانت تعرض
  المستحقّ+الجديد عبر `fsrsUpdate` فقط، وبنكٌ صغيرٌ ثابت يُستنفَد سريعاً بخلاف بنوك
  الأساسيات المولَّدة بلا حدود. **والحلّ موصولٌ لا مبنيّ من جديد**: نفس السطر الموجود
  أصلاً في `buildSpeakSession` — مراجعة حرّة من البنك كلّه إن عاد المستحقّ+الجديد فارغاً.
- **صوتٌ أقرب للبشري بلا خدمة خارجية.** `pickEnglishVoice` تُفضّل أصوات المتصفح
  السحابية (`localService:false`) والمسمّاة Natural/Neural/Enhanced/Premium/Online على
  المحلّية الآلية القديمة — من نفس قائمة الأصوات المجانية المتاحة، لا اشتراك ولا مفتاح.
  و`speakEnglish` مشتركة بين الاستماع والإملاء والنطق، فالتحسين يعمّم على الثلاثة
  بتغييرٍ واحد. **وصفحة تشخيص الصوت** (`renderAudioDiag`) تُظهر لكل صوتٍ إنجليزي على
  الجهاز شارته (سحابي/محلّي) وتُصرّح بالصوت الذي سيختاره التطبيق فعلاً على ذلك الجهاز
  بعينه — إجابة «كيف نتأكد؟» بالفتح والنظر لا بالتخمين.
- **خطأ حقيقي كشفته صورة جهاز هيا (١٦ أغسطس)**: بعض محرّكات النطق (أندرويد خاصةً)
  تُسمّي اللهجة `en_US` بشرطة سفلية لا `en-US` بشرطة عادية. والمطابقة الحرفية
  `v.lang==='en-US'` في `pickEnglishVoice` كانت تفشل بصمت على هذا الشكل، فيقع
  الاختيار على أوّل صوتٍ بالقائمة (الأسترالي عندها) لا الأمريكي المقصود — كلّها محلّية
  فلا فرق نقاطٍ يُنقذها. أُصلح بتوحيد الفاصل قبل المقارنة.

### فهم المقروء — نفس بنية الاستماع بالضبط، نصٌّ بدل صوت (١٦ أغسطس)

الثغرة نفسها التي أوجبت الاستماع: لا شيء كان يقيس فهم نصٍّ مكتوب بلا نسخه حرفياً أو
إنتاجه. والصيغة معياريّة نفسها (Cambridge YLE/KET/PET، TOEFL Junior)، فالوحدة الجديدة
**موصولةٌ بالكامل بالبنية القائمة** لا مبنيّة من الصفر: نفس `fsrsUpdate` للتباعد، ونفس
نمط `listenBankFor`/`buildListenPlan` حرفياً (`readBankFor`/`buildReadPlan`)، ونفس شكل
البطاقة والتسجيل (`domain='read'`). والفرق الوحيد من طبيعة الوسيط لا اجتهاداً: النصّ
**يبقى مرئياً** أثناء الإجابة (خلاف الصوت العابر)، فلا داعي لعرض السؤال قبله ولا لعدّاد
إعادة أو زرّ 🔊. **ودرس بنك الاستماع المستنفَد (١٥ أغسطس) طُبِّق وقايةً من أوّل يوم**:
`buildReadPlan` يحمل مراجعةً حرّة عند استنفاد المستحقّ+الجديد منذ كتابته، لا بعد شكوى
كما وقع مع الاستماع.

### التوليد يُنمّي بنكَي الاستماع/القراءة (١٦ أغسطس)

طلب صاحب المشروع: «خل المحرّك يولّد البنوك... كما فعلنا مع هيا سابقاً» — إشارةً إلى
`fetchGemini` القائمة أصلاً للفظي والعلمي. والسبب مباشر: بنك B1 الاستماع خماسي، وأعاده
محمد نفسه مرّتين خلال دقيقة واحدة (بيانات حيّة، ١٦ أغسطس) بأزمنة إجابة ٢-٩ ثوانٍ — لا
تكفي لتشغيل مقطعٍ صوتي مرّة واحدة فضلاً عن مرّتين كما سُجِّل. الأرجح حفظ موضع الخيار
الصحيح لا فهمٌ متكرّر، إذ ترتيب الخيارات ثابتٌ لكل سؤال.

**موصولٌ لا مبنيّ من جديد**: وضعٌ رابع (`mode:"gen"`) أُضيف إلى دالّة `tutor` نفسها
(لا دالّةٍ ثانية) يُنشئ عنصر استماع/قراءة واحداً بصيغة أسطرٍ موسومة (`TEXT:`/`Q:`/
`A:`/`B:`/`C:`/`CORRECT:`) — نفس أسلوب `SAY:`/`FIX:` القائم، لا JSON من نموذج (أكثر
عرضة للكسر). والعميل (`fetchTutorGen`/`parseGenBlock`) يُخزّن الناتج محلّياً في بنكٍ
ينضمّ لـ`listenBankFor`/`readBankFor`، فيدخل تناوب FSRS العادي — لا خوارزمية توليد أو
جدولة جديدة.

- **البنك المؤلَّف يبقى القاع الفوري**: `startListen`/`startRead` تُعرضان الجلسة فوراً
  من المتاح كما كانتا، والتوليد نداءٌ صامتٌ في الخلفية (`genTopUp`) يُخصّب الجلسة
  **القادمة** لا الحالية — لا شاشة انتظار ولا تغيير فيما شُحن اليوم.
- **سقفٌ لكل مستوى** (`GEN_BANK_MAX=15`): ينمو البنك حتى هذا العدد ثم يتوقّف التوليد —
  لا استهلاك طلباتٍ بلا داعٍ بعد أن يصبح البنك صحّياً.
- **فشل التوليد ليس عطلاً**: نفس فلسفة `tutor` المعلنة أصلاً — تعذّر الشبكة أو غياب
  المفتاح يعيد `null` بهدوء، والبنك المؤلَّف يعمل وحده كما كان قبل هذه الميزة.
- **التسجيل الكامل — شرط المشروع صراحةً**: العنصر المولَّد يُسجَّل بنصّه الإنجليزي
  ونصّ سؤاله وخياراته الثلاثة موسومةً بموضع الصواب (`genLogText`)، بالضبط كعُرف أسئلة
  اللفظي/العلمي المولَّدة (`«١) … | ٢) … ✓»`) — بلا هذا يستحيل معرفة ماذا وُلِّد أصلاً
  عند التشخيص لاحقاً. وعنصر البنك المؤلَّف يبقى مسجَّلاً بنصّه المعروف وحده، بلا إطالة.
- **الجولة الحيّة الأولى (١٦ أغسطس، دقائق بعد الشحن)**: عناصر مولَّدة فعلاً وصلت
  `mawhiba_listen_aibank` وأُجيب عنها عند إلياس ومحمد معاً — التوليد يعمل. لكن كشفت
  عيبين حقيقيين: (١) تكرار السيناريو نفسه («A: I'm going to the library at 3
  o'clock…») **أربع مرّات متتالية** عند إلياس رغم تعليمة «نوّع المواضيع» — تعليمة نصّية
  وحدها لم تكفِ نموذجاً بلا ذاكرة بين النداءات. (٢) خطأ تذكير/تأنيث في سؤالٍ عربي
  («ما **هي** الوقت» بدل «ما **هو** الوقت»).
- **العلاج بذرة موضوع لا تعليمة مرجوّة**: `GEN_TOPICS` قائمة ٢٩ موضوعاً تُختار عشوائياً
  في الخادم نفسه (`systemGen`) وتُفرَض صراحةً في كل نداء («TOPIC FOR THIS ITEM: …») —
  إجبارٌ بنيوي على التنويع لا أملٌ في التزام نموذجٍ عديم الذاكرة. **وُصلح التكرار فعلياً**
  (تحقّق لاحقاً من بيانات حيّة)، أمّا قاعدة تذكير/تأنيث العربية فصارت بلا موضوع بعد
  التحويل التالي إلى الإنجليزية بالكامل — لم تعد المشكلة قائمة أصلاً.

### الأسئلة والخيارات بالإنجليزية للثلاثة — لا عربية (١٦ أغسطس)

كانت أسئلة الاستماع/القراءة وخياراتها عربية للثلاثة منذ يوم الإطلاق — تبريرها الأصلي:
تجنّب خلط قدرتين لدى A1 المبتدئة (فهمٌ مسموع + قراءة إنجليزية). وطلب صاحب المشروع نصّاً
حرفياً من كامبريدج للتحقّق (بعد أن سألته صيغة الاختبار)، فجلب اقتباساتٍ مباشرة من
`cambridgeenglish.org` تُثبت: أوراق كامبريدج الحقيقية (YLE، A2 Key، B1 Preliminary) تُطبع
**بالإنجليزية بالكامل بلا ترجمة لأيّ لغة أمّ** — «Most Cambridge English exams have a
Listening test, where candidates: read questions which are printed on a question paper»
بلا إشارة لأيّ لغةٍ غير الإنجليزية. اقترحتُ تطبيق هذا على محمد (B1) وحده تحفّظاً على
هيا (A1)، **فردّ صاحب المشروع: للجميع لا لمحمد وحده** — قرارٌ صريح يتجاوز تحفّظي.

**التنفيذ**: `LISTEN_BANK`/`READ_BANK` (٥٠ عنصراً) تُرجمت بالكامل — `q` و`c` إنجليزية،
`audio`/`passage` كانت إنجليزية أصلاً فلم تتغيّر. والعرض (`renderListen`/`renderRead`)
أضاف `direction:ltr;text-align:left;font-family:Georgia,serif` على `.q` وأزرار الخيارات
— نفس نمط كل نصٍّ إنجليزي آخر في التطبيق (بطاقات المحادثة، فقرة القراءة نفسها).
والتوليد الآلي (`systemGen` في `supabase-functions-tutor.ts`) صار يطلب Q/A/B/C إنجليزية
لا عربية، وحُذفت قاعدتا تذكير/تأنيث العربية وحياد الجندر (لم تعودا تخصّان شيئاً).
`parseGenBlock` وُسِّع: تلوّثٌ عربي في أيّ حقل (لا `passage`/`TEXT` وحده كسابقاً) يُسقط
العنصر كلّه — العنصر بأكمله إنجليزي الآن أو لا يُقبل.

- **مرّتان استماعٍ للثلاثة سواءً — تصحيحٌ لا ميزةٌ جديدة**: `listenPlaysHint` كانت تُفرّق
  B1 بمرّة واحدة قياساً على IELTS. والاقتباس نفسه الذي جلبه صاحب المشروع كذّب هذا: B1
  Preliminary تنصّ حرفياً «You hear each recording twice» — نفس YLE وA2 Key بلا استثناء.
  IELTS اختبارٌ مختلف تماماً لا صلة له بـB1 Preliminary. صُحِّح إلى مرّتين للثلاثة —
  إرشادٌ نصّي لا قفلٌ تقني كما كان.
- **لم يُقَس أثره بعد**: هل قراءة سؤالٍ إنجليزي تُبطئ هيا (A1) أو تُربكها مقارنةً بالعربي
  السابق؟ لا بيانات حيّة بعد التحويل — يُقاس بدقّة كل مستوى (`domain='listen'/'read' and
  qtype='A1'`) عبر الجلسات القادمة، لا يُفترض.

### الكتابة: إنتاجٌ حرّ لا اختيارٌ من متعدد (١٦ أغسطس)

الاستماع والمقروء كلاهما فهمٌ، وبنك الكلمات (`wb*`) يقيس ترتيب كلماتٍ جاهزة — لا شيء
كان يطلب منها تأليف نصٍّ من الصفر، وهو بالضبط ما كشفه القياس عندها (متوسّط جملتها في
المحادثة ١٤ حرفاً، ٤ من ١٥ نسخٌ حرفيّ). فقسمٌ جديد (`WRITE_BANK`) يطلب نصّاً كاملاً
تكتبه هي، بصيغة Cambridge المعياريّة نفسها: Movers/Flyers (A1) أسئلة توجيهية عن صورة
غيرِ متاحة هنا فوصفٌ نصّي يحلّ محلّها (كما حلّ النصّ محلّ الصوت في LISTEN_BANK)، وA2 Key
Part 6 (A2) رسالة قصيرة بثلاث نقاط محتوى مفروضة (٢٥-٣٥ كلمة)، وB1 Preliminary Part 2
(B1) قصّة أو رسالة قصيرة (٨٠-١٠٠ كلمة) بلا نقاط مفروضة.

**موصولٌ لا مبنيّ من جديد بالكامل**: نفس `fsrsUpdate` للتباعد، ونفس مسار مراجعة
المحادثة بالضبط (`mode:"review"` في tutor، محكوماً بـLanguageTool) للتصحيح النحوي —
لا مسار تصحيح ثانٍ. وتصحيحاته تدخل بطاقات «أخطائي» (`fixCardSave`) كتصحيحات المحادثة
سواءً بسواء، فبطاقة نوعٍ جديد لم تُبنَ. والنجاح **يُقاس لا يُفترض**: لا «صواب» واحد
لنصٍّ حرٍّ، فالمقياس عدد الكلمات (بلغت الحدّ الأدنى المعياري لمستواها أم لا) — قابلٌ
للقياس فعلاً، بخلاف «جودة» الإنتاج التي تبقى تصحيحاً معروضاً لا درجة تُحسب عليها.

- **جلسةٌ قصيرة عمداً** (`WRITE_N=3`، لا ٨ كالاستماع/المقروء): كتابة نصٍّ كاملٍ أثقل
  من اختيار إجابة.
- **الحكم عليه لم يقع بعد**: عدد الكلمات كل جلسة (`domain='write'`)، ونسبة التصحيحات
  التي تدخل «أخطائي» ثم تُتقَن — وهل تنتقل تصحيحات الكتابة إلى المحادثة أو العكس، لا
  بيانات حيّة بعد الشحن.

**مثالٌ محلول اختياري قبل الكتابة (`WRITE_MODEL`، ١٧ أغسطس)** — سُئلت: هل يُعلَّم أسلوبٌ
أفضل بإجابة نموذجية؟ والجواب الأصليّ كان لا: التصحيح بعد الإرسال يُصلح خطأً نحوياً في
جملتها هي (recast)، ولا يعرض فقرة بديلة. لكن هذا فجوةٌ حقيقية مقابل الأدبيات: **بيداغوجيا
الجِنس النصّي** (مدرسة سيدني، Rothery وMartin) توصي بدورة «تفكيك نصٍّ نموذجي ← بناء
مشترك ← بناء مستقل» — والمكان المعياري لعرض نموذج هو **قبل** الكتابة الحرّة لا بعدها.
فأُضيف زرٌّ اختياري (`writeToggleModel`) يعرض مثالاً محلولاً واحداً لكل مستوى — **لموضوعٍ
آخر غير سؤالها هي** عمداً، فعرض إجابةٍ جاهزة لنفس سؤالها كان سيُخاطر بأن تُنسَخ حرفياً
بدل أن تُنتَج (نفس درس «الدعم الذي يُعلّم خطأً أسوأ من غيابه» — اقتُرحت عليها جملة مكسورة
فكرّرتها كما هي). موصولٌ لا مبنيّ من جديد: نفس شكل صندوق «مثال محلول» في وضع التقوية
(`fadeStage===1`، `#EFF6FF`/`#1E40AF`/📘) بلا لونٍ أو اسمٍ جديد. **ولا يُقفَل ولا يُفرَض**
— إرشادٌ اختياري كزرّ إعادة الاستماع 🔊، ويُسجَّل عدد مشاهداته (`writeModelViews`) مع
كل محاولة (`q_text` ⇐ `مثال:N`) ليُقاس أثره لاحقاً: هل من يفتحه يبلغ هدف الكلمات أكثر
ممّن لا يفتحه؟ لا بيانات حيّة بعد.

### دقّة القواعد: اختاري الجملة الصحيحة من أربع (١٧ أغسطس)

طلب صاحب المشروع تدريباً داخل قسم الكتابة: أربع جملٍ، صحيحةٌ واحدة والباقي أخطاء. طلبتُ
منه مثالاً لكل مستوى قبل النشر ليعتمد الطريقة — فبُحث الأساس أوّلاً لا بُني حدساً:
الصيغة نفسها اسمها **Grammaticality Judgment Task**، أداة قياسٍ معياريّة في أبحاث
اكتساب اللغة الثانية (Spinner & Gass، Routledge؛ فصل Cambridge Core "Grammaticality
judgment tasks") — لا صيغة مخترعة. ومحتوى كل مستوى من **English Profile** (مشروع
كامبريدج الذي يحدّد التراكيب "المُميِّزة" لكل درجة CEFR) ومن أخطاء المتعلّمين
الموثَّقة لكل درجة: نسيان -s مع الغائب (A1)، وتسوية الأفعال الشاذّة في الماضي
(goed بدل went، A2)، والمضارع التامّ — نقطة التعثّر الموثَّقة عالمياً عند B1 تحديداً
("B1 is where many learners plateau... the present perfect trips up speakers whose
first language has no equivalent" — والعربية فعلاً بلا زمنٍ تامّ مطابق).

**شرح كل خيارٍ لا الصحيحة وحدها**: عندما سُئل عن طريقة الشرح، اقتُرحت أربعة بدائل
(سببٌ واحد مختصر / تفكيك الأربعة / تلوينٌ بلا شرح / قاعدة+مثال بصري) واختار
المُوصى به: تفكيك الأربعة بعد القفل، سطرٌ سببٌ لكلٍّ. **أقرب لمبدأ «شكل الخطأ لا
وقوعه» المعتمد أصلاً في المشروع** — تُعلَّم عائلة الخطأ لا الجواب وحده، ولا يحتاج
بناء واجهة جديدة (نصٌّ إضافي تحت الخيارات الأربعة). وكل مموّهٍ في `GRAM_BANK` يعزل
آليةً واحدة محدَّدة (خلط be بالفعل الأساسي، عدم تطابق الجمع، since/for، إلخ) لا
تشويشاً عشوائياً — نفس انضباط مموّهات `mcNum` الأخرى في المشروع.

**نفس بنية الاستماع/القراءة/لعبة الاستماع بالضبط**: `GRAM_BANK` بمستويات (٥ لكلٍّ من
A1/A2/B1)، و`fsrsUpdate` للتباعد، وشاشة نتيجة — لا آلية جديدة. وترتيب عرض الخيارات
الأربعة يُخلَط كل جولة (`gramSetupRound`) — لا انحياز موضع، كنفس مبدأ لعبة الاستماع.
ظهرت للثلاثة سواءً بجوار زرّ الكتابة. **لم يُقَس أثره بعد** — لا بيانات حيّة.

### نمط أسئلة اختبار STEP للثلاثة — ١٨ أغسطس

عرض صاحب المشروع صفحة تدريبٍ حقيقية على STEP وقال: «أضف نوعية هذه الأسئلة للثلاثة كلٌ
حسب مستواه مع الشرح بعد الإجابة».

**والبنية من الاختبار نفسه لا من حدسي**: قسم القواعد ثاني أكبر أقسام STEP (**٤٠٪**)،
ويشمل «التحليل الكتابي» — وهو حرفياً: علامات الترقيم، وترتيب الجمل، واختيار الجملة
الأصحّ، والكبتلة (مصدر: qiyas.sa وأدلّة STEP). فالأنواع الثلاثة في `STEP_BANK` ليست
تقسيماً اخترعته بل ما يحويه القسم فعلاً:

| النوع | يقابل في الاختبار |
|---|---|
| `pick` | اختيار الجملة الأصحّ · ترتيب الكلمات · الكبتلة · الترقيم |
| `gap` | الفراغات النحوية (الأزمنة، حروف الجرّ، أدوات الربط، صيغة السؤال) |
| `order` | ترتيب الجمل في فقرة |

**موصولٌ لا مبنيّ من جديد**: نفس `fsrsUpdate` للتباعد، ونفس `logAnswer` وأعرافه، ونفس
شكل أزرار الخيارات وشاشة النتيجة. ونوع `pick` **مطابقٌ عمداً لشكل `GRAM_BANK`** (أربع
جملٍ وشرحٌ لكلٍّ بعد القفل) لأن سؤال «اختر الجملة الأصحّ» في STEP هو هو.

**ولماذا قسمٌ مستقلّ لا توسيعٌ لـ`GRAM`** — وقد ترجّح التوسيع أوّلاً: (١) `GRAM` أداة
قياسٍ معياريّة مسمّاة (Grammaticality Judgment Task) وخلطُ صيغة امتحانٍ بها يُفسد ما
تقيسه؛ (٢) نوعا الفراغ والترتيب لا يقبلهما شكل `GRAM` أصلاً؛ (٣) `domain='step'` منفصلٌ
يجعل الاستعداد للاختبار قابلاً للقياس وحده.

- **ومكوّن الترتيب لم يُعَد استعماله رغم وجود نظيره** (`wb*`، ترتيب الكلمات بالضغط):
  ذاك مربوطٌ بسلّم الإملاء الثلاثي وبمموّهاته وبالكلمات لا الجمل، وإقحام الجمل فيه كان
  سيُفسد الاثنين. فالمُعاد أسلوبه البصري (اضغط لتُضيف، اضغط المُختار لتحذف) لا دالّته —
  ويُقال هذا صراحةً بدل ادّعاء وصلٍ لم يقع.
- **الشرح منهجٌ لا جواب**: عناصر `order` تشرح **كيف يُحلّ النوع** — ابحث عن رأس الفقرة
  (الجملة التي لا تشير إلى ما قبلها ولا تحمل كلمة تسلسل) ثم رتّب بكلمات الإشارة
  (`To start`/`Then`/`Eventually`). وهذا امتدادٌ لمبدأ «شكل الخطأ لا وقوعه»: تُعلَّم
  الآلية لا الإجابة.
- **الجلسة تخلط الأنواع** كما يخلطها الاختبار (`STEP_N=6`)، **وتضمن عنصر ترتيبٍ واحداً
  على الأقلّ** — هو أثقل الأنواع وأقلّها عدداً، فلولا الضمان لغاب عن أكثر الجلسات
  بحكم الاحتمال وحده.
- **وترتيبها المُختار يُسجَّل تسلسلاً** (`3-1-2-4`) لا صواباً/خطأً فقط — فيُعرف **أين**
  اختلّ الترتيب لا أنه اختلّ، بنفس عُرف «شكل الخطأ لا وقوعه». وكل عنصرٍ موسومٌ بمهارته
  (`[الماضي البسيط]`، `[الترقيم]`…) فتُقاس كل مهارة على حدة.
- **وهيا (١١، A1) ليست من مُتقدّمي STEP** — الاختبار للجامعة، ومحمد (١٩، B1) هو المعنيّ
  فعلاً فبنكه أقرب ما يكون لصعوبته الحقيقية. لكن صيغته (ترتيب الكلمات، حروف الجرّ،
  الكبتلة) تدريبٌ نافع في مستواها، فبنكها مكتوبٌ **بمفرداتها لا بمفردات الاختبار**.
  وهذا قرار صاحب المشروع صراحةً («للثلاثة كلٌ حسب مستواه»).

**لم يُقَس أثره بعد**: دقّة كل نوعٍ على حدة (`domain='step'` و`qtype='pick'/'gap'/'order'`)
عبر الجلسات القادمة — وأيّ المهارات الموسومة أضعف. ولا يُدَّعى أنه يرفع درجة STEP حتى
تُقاس درجةٌ حقيقية.

### لعبة الاستماع: أزواج شبه متطابقة (minimal pairs) — ١٦ أغسطس

طلب صاحب المشروع تنفيذها مباشرةً بعد لقطة شاشة من تطبيقٍ آخر (كلمة تُسمع، يُختار بين
`chose`/`choose` بنُطقَيهما)، مع إضافةٍ صريحة منه: عرض الكلمة داخل جملة بسيطة بعد
الإجابة لتعليم معناها. **نفس بنية الاستماع/القراءة بالضبط** — `MINPAIR_BANK` بمستويات
(A1/A2/B1، ٥ لكلٍّ)، و`fsrsUpdate` للتباعد، وشاشة نتيجة — لا آلية جديدة. والفرق من
طبيعة التمرين لا اجتهاداً: هدفان محتملان لا واحد (يُختار أحدهما عشوائياً كل جولة
`minpairSetupRound`، وترتيب العرض يُخلَط أيضاً — لا انحياز موضع)، وبعد القفل تُعرض
الكلمة الصحيحة داخل جملتها (نصّاً وصوتاً عبر `speakEnglish` نفسها).

**الأزواج معياريّة من تدريس النطق الإنجليزي** لا مؤلَّفة اعتباطاً: اختلاف الحركة
الطويلة/القصيرة (`ship/sheep`، `bit/beat`)، وصوت النهاية (`live/leave`)، ونمط اسم/فعل
(`advice/advise`، ومثال صاحب المشروع نفسه `chose/choose`) — تصنيفاتٌ قياسية في تعليم
النطق، لا تخمين. **ظهرت للثلاثة سواءً** بنفس منطق الاستماع/القراءة (زرّ «👂 لعبة
الاستماع» على صفحات الأخوين وهيا معاً) — لا سبب لتخصيصها بمستوًى واحد.

**لم يُقَس بعد**: هل الأزواج الأصعب (B1، نمط اسم/فعل) صعبةٌ فعلاً بلا فهمٍ سياقي، أم
يكفي السياق (الجملة الشارحة بعد الإجابة) لتثبيت الفرق مع التكرار؟ يُحكَم عليه بدقّة كل
مستوى (`domain='minpair'`) عبر الجلسات القادمة.

**ثغرة معنى حقيقية — درس ١٧ أغسطس**: الشحنة الأولى عرضت الكلمة داخل جملةٍ إنجليزية
وحدها بعد الإجابة، بلا معنى عربي — سأل صاحب المشروع «ليش ما حطيت معاني الكلمات
بالعربية؟». والسبب فسّرته بنفسي: تطبيق «شرح المعنى» بسياقٍ إنجليزي وحده تجاهل عُرفاً
قائماً أصلاً في التطبيق لنفس الغرض (شرائح «🔊 عربي = English» في وضع التقوية) — بناءٌ
موازٍ بدل وصلٍ بما هو موجود. أُصلح بإضافة `m:[معنى١,معنى٢]` لكل زوج في `MINPAIR_BANK`،
تُعرض الآن كسطرٍ «المعنى = word» فوق الجملة مباشرة — فالسياق يُرسّخ الاستعمال، والترجمة
تضمن وصول المعنى نفسه، خصوصاً لهيا (A1) التي قد لا تفهم الجملة الإنجليزية أصلاً.

### التوليد لبنكي الكتابة ولعبة الاستماع، ومراجعةٌ محكومة بحسب الخطر — ١٧ أغسطس

قال إلياس صراحةً: «الاختبارات متكررة» و«لا توجد ترجمة لكلمات لعبة الاستماع». والفحص
أثبت الأولى فعلاً لا انطباعاً: خمس عشرة محاولة `minpair` في يومٍ واحد على بنكٍ من
**خمسة** أزواج فقط لكل مستوى — يُعاد الزوج نفسه حتماً خلال الجلسة. والثانية فجوةٌ
حقيقية: صندوق «بعد القفل» يَعِد بتعليم المعنى («لتتعلّمي معناها») ولا يترجم شيئاً.

**طُلب توليدٌ آليّ «لكل الأقسام»، والرقابة (قبلية أم بعدية) تُركت لاجتهادي.** فحُصت
كل قسمٍ بلا بنك توليد بعد (`COACH_SCENES`، `ENG_BUILD`، `DICTATION_A1`) — أحجامها
مقاربةٌ أو أكبر من قاعدة الاستماع/القراءة الأصلية (٧-٢٦ عنصراً) وليست مستنفَدةً كما
كان بنك الاستماع في ١٥ أغسطس، فلم تُمسّ. اثنان فقط استحقّا التدخّل الآن: **الكتابة**
(بنكٌ ثابتٌ صرفٌ، ٣-٥ لكل مستوى، لا توليد إطلاقاً) و**لعبة الاستماع** (السبب المباشر
لشكوى إلياس).

**والرقابة فُرِّقت بحسب الخطر لا بقاعدةٍ واحدة للجميع** — فالمكوّنان يختلفان في ما
يمكن أن يُخطئه النموذج:

- **الكتابة ونصّ فقرات لعبة الاستماع الشارحة: أيّ نصٍّ إنجليزي متماسك يصلح**، فالرقابة
  **بعدية** كنمط `LISTEN_BANK`/`READ_BANK` القائم أصلاً: يُطلب من `tutor` (وضعٌ رابعٌ
  موسَّع `domain:"write"`) نصّ Cambridge (Movers/Flyers لِA1، A2 Key Part 6، B1
  Preliminary Part 2 بحسب المستوى، بموضوعٍ من `GEN_TOPICS` نفسها المستخدَمة أصلاً
  لتنويع الاستماع/القراءة)، فيتحقّق العميل بعد الاستلام (`parseGenWriteBlock`): لا
  تلوّث عربي، حدٌّ أدنى معقول للكلمات، والشكل الموسوم (`PROMPT:`/`MIN:`) سليم — وإلا
  يُسقَط العنصر بصمت كما تُسقَط عناصر الاستماع/القراءة المكسورة.
- **زوج minimal-pair نفسه (أيّ كلمتين تتقابلان صوتياً) رقابته أصعب من أن تُتحقَّق
  آلياً** — لا وسيلة للتحقّق من صحة تقابلٍ صوتي بلا صوت. فبقي `MINPAIR_BANK` (تضاعف
  الآن ١٠ أزواج/مستوى، ٣٠ إجمالاً، بيدي) **مؤلَّفاً بشرياً بالكامل ولا يُولَّد قطعاً** —
  يمتنع النموذج عن اختيار الأزواج تماماً كما تمتنع مولّدات quant عن تخمين آليّات خطأٍ
  لم يثبتها القياس. **والتوليد يقتصر على جملةٍ مثالٍ إضافية لزوجٍ موجودٍ أصلاً**
  (`minpairGenTopUp`/`fetchTutorGenMinpair`) — خطرٌ أقلّ من اختيار الزوج نفسه، ومع ذلك
  رقابته **قبلية**: تحقّقٌ بحدود الكلمة (`\bword\b`) يضمن ظهور الكلمة الهدف بصيغتها
  المطلوبة بالضبط قبل قبول الجملة، لا بعد عرضها — فالجملة المرفوضة لا تُخزَّن أصلاً،
  خلاف الكتابة حيث الفشل يُسقَط بعد التخزين المؤقّت لا قبله.

**موصولٌ لا مبنيّ من جديد**: كلا المسارين يستعمل `genTopUp`/`genBankLoad`/`genBankAdd`
نفسها (سقفٌ `GEN_BANK_MAX=15` للكتابة) أو نظيرتها المحلّية لِminpair (`MINPAIR_EXTRA_MAX=3`
لكل كلمة)، ونداءً صامتاً بعد بدء الجلسة (`writeGenTopUp`/`minpairGenTopUp` في
`startWrite`/`startMinpair`) يُخصِّب الجلسة **القادمة** لا الحالية — لا شاشة انتظار.

**والترجمة أُضيفت مباشرةً بلا انتظار توليد** — وقد بنتها جلستان متوازيتان في اليوم
نفسه بحقلين مختلفين (`tr` هنا و`m` هناك). الدمج أبقى **حقلاً واحداً `m`** (الأسبق إلى
`main`، وعليه يقوم عرضه واختباره) مع **الأزواج الثلاثين** (١٠/مستوى) من هنا — لا
حقلان لمعنًى واحد. وهذا بنفسه درسٌ في العمل المتوازي: التعارض لم يكن في المنطق بل في
الاسم، ولا يكشفه git وحده لأن كلا الطرفين «صحيح».

**اختُبر قبل الشحن**: `tests/test_minpair.js` (بنكٌ موسَّع، حقول `tr`، توليد الجملة
ومسارها في العرض) و`tests/test_write.js` (توليد الكتابة، `writeBankFor` مع مصدرين).
والخادم (`supabase-functions-tutor.ts`، `systemGenWrite`/`systemGenMinpair`) نُشر
مباشرةً (نسخة ٢٠).

**لم يُقَس بعد**: هل تنوّع أزواج minimal-pair (١٠ بدل ٥) وتوليد الكتابة يُخفّضان تكرار
الشكوى فعلياً؟ يُحكَم عليه بتوزيع `q_text`/`response` عبر الجلسات القادمة، لا يُدَّعى.

### بنك الكلمات ووضع «أخطائي»

**القياس أوّلاً، ثم العلاج.** ما أوجب البنك: متوسّط جملة هيا في المحادثة **١٤ حرفاً**،
و**٢١ من ٢٤** جملة أربع كلمات فأقلّ، و**٤ من ١٥** دوراً نسخٌ حرفيّ للمقترح. ومحمد
(٦٢ حرفاً) وإلياس (٢٥) **صفرٌ من سبعة** لكليهما — فالعلّة لها وحدها.

- **`wb*`** مكوّن عامّ: جملةٌ تُبعثر كلماتها فتُرتَّب. ثلاث درجات — بالعدد بالضبط ⇐
  كلماتٌ دخيلة ⇐ بلا بنك. صعودٌ بثلاث صحيحات متتالية ونزولٌ بخطأين، كسلّم الإملاء.
  والدخيلة **لا تُطابق كلمةً في الجملة** وإلا صار الصواب غامضاً.
- **`ENG_BUILD`** بنك جملٍ واحد لثلاثة أبواب: الإملاء (تُسمَع ثم تُبنى)، وخطة
  الإنجليزية (تُطلب بالعربية)، ووضع أخطائي. وجمله في مخزون FSRS بمعرّفاتها.
- **بوّابة الصدى** (`echo*`): البنك في المحادثة **مشروط**. من ينسخ مرّتين في آخر
  ستّة أدوار تُسلَّح عليه، ومن يؤلّف تنحلّ عنه. وبذرة هيا خلاصةُ قياسٍ وقع
  (`seed:true`) لا أدواراً مخترعة — وأوّل دورين جديدين يحكمان.
- **وضع «أخطائي»** (`mode="errors"`): مكانٌ واحد يجمع بطاقات تصحيح المحادثة
  (`mawhiba_coach_fix_v1`، مجدولة بـFSRS) وكلمات الإملاء المخطئة وأسئلة الخطة
  العالقة. **وقوالب موهبة تُذكَر ولا تُدرَّب فيه** — إعادتها مبنيّة في الاختبار
  (`qzInjectDue`)، وبابان على العدّاد نفسه يتنازعان.
  وبطاقة التصحيح لا تعود في يومها (عُرضت في الخلاصة)، وتُحذف بعد ثلاث إصابات.
  وهو الباب الوحيد الذي ينفع الثلاثة سواءً: الأخوان لا يريان إلا المحادثة.

### ثلاثة مكوّنات مجرَّبة بدل ما نبنيه بأيدينا

القاعدة الثالثة («لا بناء علاج من الصفر») تنطبق على البنية لا على الأسئلة وحدها.
فالمجرَّب المفتوح يسبق المكتوب هنا:

- **FSRS-6** بدل سلّم التكرار الثابت (`SRS_INTERVALS`). خوارزمية مفتوحة مُعايَرة
  على مليارات المراجعات، تحسب استقراراً وصعوبةً واسترجاعاً لكل بطاقة بدل صناديق
  ثابتة. الأوزان الاثنان والعشرون في `FSRS_W`، والدوالّ `fsrsUpdate`/`fsrsIvl`
  مُقابَلة بـ`ts-fsrs@5.4.1` في `tests/test_fsrs.js`: ٦٠٠ قيمة في ١٠٠ حالة كلّها
  مطابقة. **ولا تُحصر `fsrsInitD`** — المرجع يُعيد −٤٫٧٧ لدرجة Easy، والحصر
  يقع بعدها في `fsrsClampD`.
- **LanguageTool حَكَماً** على تصحيحات النموذج في وضع المراجعة. مدقّق قائم على
  قواعد لا على نموذج: لا يخترع. كل جملة «صحيحة» يقترحها النموذج تُعرض عليه، فإن
  وجد فيها خطأً أُسقطت البطاقة. **وهو حَكَمٌ لا بديل**: تعذُّر الوصول إليه لا
  يُسقط شيئاً — لا نُعطّل التصحيح لعطلٍ فيه. ويُسجَّل `lt:المحكوم/المُسقَط`.
- **speechocean762 مسطرةً** لدرجات النطق: ٥٠٠٠ جملة من ٢٥٠ متعلّماً، لكلٍّ تقييم
  خمسة خبراء. الحكم: ارتباط سبيرمان ≥ ٠٫٧ يعني أن الدرجة تصلح للتشخيص.
  **لم يُشغَّل بعد** — بيئة التطوير تحجب `openslr.org` وSupabase وLanguageTool،
  فالمقياس مكتوب ينتظر جهازاً متّصلاً. وما لم يُقَس لا يُدَّعى.

**مشروع Supabase:** `yzznkerweqkmhrtbidrl` (mawhiba-training).
**الجدولان:** `mawhiba_attempts` (نتيجة كل محاولة) و`mawhiba_answer_log` (سطر لكل إجابة).

### أعراف التسجيل — عليها يقوم كل تشخيص

- `q_text` نصّ السؤال، و`response` إجابتها. **بلا هذين لا تشخيص.** وقد بقي وضع
  التقوية ثلاثة أسابيع يُسجّل درجات بلا سؤال ولا جواب، فكان يُقال «٧ أخطاء من ١٠»
  ولا يُعرف في أيّ سؤال.
- الإجابة تُسجَّل ومعها الصواب في سطر واحد: `«٧٠٠٠ · الصواب ٦٠٠٠»`.
- الخيارات المرسومة تُسجَّل **بموضعها** لا بشيفرتها: `«موضع ١ · الصواب موضع ٣»` —
  فينكشف انحياز الموضع، ولا يدخل SVG في السجلّ.
- ما اختارته من أوضاع يُسجَّل كذلك (`help:suggest` مثلاً) — لتُقاس فائدة الدعم.
- **شكل الخطأ لا وقوعه**: `«٢٥ · الصواب ٢٦ · فرق −١»`. بلا الفرق تُقرأ خمس عشرة
  إجابة خاطئة على أنها ضعفٌ عامّ، ومعه تنكشف ثلاث آليات بأعيانها.
- **خيارات الأسئلة المولَّدة** تُسجَّل كلّها مع نصّ السؤال وموضعُ الصواب موسوم
  (`الخيارات: ١) … ✓ | ٢) …`). وقد أجابت عن سؤالين منطقيين مختلفين بالجواب نفسه
  «يذهبون إلى المستشفيات» — وهو لا معنى له في سؤال الصيّادين — ولم يكن يمكن الحكم:
  أخللٌ في التوليد أم انحيازُ موضع؟ والرسوم تُسجَّل `[رسم]` لا شيفرةً.

## دروس مدفوعة الثمن

- **Azure REST يُعيد درجات النطق مسطّحة على `NBest[0]`، وSDK يُعيدها متداخلة تحت
  `PronunciationAssessment`.** أسبوع ضاع في مطاردة «عطل في الخدمة» وكان الخطأ في
  القراءة. اقرأ الشكلين معاً (`paOf`).
- **الدعم الذي يُعلّم خطأً أسوأ من غيابه.** اقتُرح عليها `I went beach` فقالتها.
  كل جملة تُعرض لتُقال يجب أن تكون سليمة نحوياً.
- **الدعم الذي لا يتلاشى يصير عكازاً.** ثماني أسئلة باختيارين ⇒ ثماني إجابات
  صداها. لكل دعمٍ خطّةُ تخفيف.
- **المراقب (MutationObserver) لا يمرّ على `<script>`** — مئات الكيلوبايتات عند كل
  ضغطة، حتى مات السياق.
- **ما لا يُسجَّل الآن لا يُشخَّص لاحقاً.** الماضي لا يُصلَح بأثر رجعي. ومن ذلك
  **نتيجة** العملية لا مخرجاتها فقط: كنت أُسجّل التصحيحات ولا أُسجّل أن المراجعة
  جرت، فرأيتُ جلسةً بلا تصحيحات ولم أعرف: أهي بلا أخطاء أم فشل الطلب؟
- **خطأ التفريغ يصير تصحيحاً لخطأ لم تفعله.** اقتُرح عليها `I had toast.` فقالتها،
  فسمعها Whisper `I had dust` فردّ الشريك «I don't think dust is food». والجملتان
  المعروضتان في يدنا، فتُقابَل بهما (`snapToSuggestion`).
- **ما يُبنى للنهاية لا يصل إلى من لا ينتهي.** إلياس ثلاث جلسات ولم يرَ صفحة التصحيح
  ولا مرّة — يخرج من الجلسة كما هي، والمراجعة عند «إنهاء» أو بعد ثماني جمل. فصارت
  **مراجعةٌ صامتة عند الجملة الرابعة** تحفظ البطاقات، و**الخروج يمرّ على الخلاصة**
  لمن تكلّم (`coachLeave`). ومن لم يتكلّم يخرج بلا اعتراض.
- **حالة الفشل تُعرض ولا تُسجَّل ⇒ لا تُشخَّص.** ماتت جلسة إلياس بلا جملة واحدة ولم
  أعرف: أميكروفون أم تفريغ أم تركها؟ فصار `coachSetFail` يُسجّل النوع ومعه كم جملة
  قيلت قبله ودليلُ Whisper نفسه (`noSpeech`).
- **والتفصيل نفسه قد يصل ويُهمَل قبل أن يُسجَّل — درس ١٧ أغسطس.** `tutor_bad_model`
  ضرب إلياس ومحمد معاً خمس مرّات خلال دقائق في محادثة حيّة، والسجلّ كله يقول
  «reply · tutor_bad_model» بلا اسم النموذج المرفوض ولا نصّ رفض المزوّد — رغم أن
  الخادم كان يُعيدهما في الردّ نفسه (`d.detail`/`d.provider`/`d.model`) في كل مرّة.
  لم يكونا غائبين، بل مُهمَلين: السطر `coachSetFail(...,(d&&d.error)||null)` كان
  يمرّر شيفرة الخطأ وحدها لا الكائن كلّه. وتعذّر التشخيص من مصدرين آخرين جرّبتهما
  فعلاً: سجلّات Supabase للدالّة لا تحمل جسم الردّ (تُظهر ٢٠٠ فقط لأن `jsonOut`
  يُعيد ٢٠٠ حتى عند فشلٍ داخلي)، والاتصال المباشر بالدالّة من بيئة التطوير محجوبٌ
  بسياسة الشبكة. **فالمصدر الوحيد المتبقّي كان العميل نفسه** — وكان يملك التفصيل
  بيده ويرميه. أُصلح بتمرير `d.error`+`d.provider`/`d.model`+`d.detail` معاً.
- **والتشخيص المُصلَح كشف السبب فعلاً، وفتح ثانياً بعده — الدرس نفسه يتكرّر.**
  السجلّ بعد الإصلاح أظهر أن Groq أوقفت `llama-3.3-70b-versatile` نهائياً في اليوم
  نفسه (١٦ أغسطس)؛ بُدِّل إلى `openai/gpt-oss-120b` (بديل Groq الموصى به) ونُشر.
  لكن محمد جرّب مباشرةً بعدها وعادت الرسالة نفسها — خطأً مختلفاً هذه المرّة
  (`tutor_no_text`) كشفه التشخيص نفسه فوراً بدل أن يضيع كما ضاع `tutor_bad_model`
  قبله. والسبب: `gpt-oss-120b` نموذج تفكير يُنفق من سقف الرموز (`max_tokens`) على
  تفكيرٍ داخلي قبل الجواب — وسقف المحادثة الاعتيادي (٣٠٠) كان يُستنفَد تفكيراً
  كلّه، فيعود جواب فارغ (`finishReason:"length"`، مؤكَّدٌ من مصدر Groq نفسه).
  أُصلح بخفض `reasoning_effort` إلى `low` ورفع السقف إلى ١٢٠٠ لهذا النموذج تحديداً
  — وأُضيف `finishReason` أيضاً لسطر `coachSetFail` كي لا يتكرّر فقدان تفصيلٍ جديد.
- **التصحيح بعد السمع لا يُمسك كلمةً سليمة.** سُمع لإلياس «I need an address for my
  sister» فبنى الشريك أربع دقائق على بطاقات المعايدة، والأرجح أنه قال «a dress».
  و`address` كلمة إنجليزية صحيحة فلا تُستبدل بالحدس. **فالعلاج قبل السمع**: كلمات
  الموقف (`COACH_SCENES[].words`) تُمرَّر تحيّزاً لأذن Whisper (`hint`).
  **وهذا لا ينقض قرار «لا prompt»** — ذاك عن الجملة المستهدفة في تمرين النطق، وهو
  «نجاح كاذب»؛ وهذا مجالُ حديثٍ بلا كلمات هدف، ويُسقَط الحقل قطعاً متى وُجدت
  `targetWords`. ويُعاد `hinted` في الردّ لتُقاس فائدته لا تُدَّعى.
- **درجة النطق تكذب حين يُخطئ التفريغ.** جملة سجّلت ١٠٠٪ ومعها أصوات بدرجة صفر:
  Whisper سمع غير ما قيل، فقاس Azure على نصّ لم يُنطَق. فلا يُسمّى صوتٌ ضعيفاً إلا
  إذا ضعف في كلمتين، ولا يُصدَّق صفرٌ مع درجة عالية. والخام يبقى في السجلّ كما هو —
  التصفية على العرض لا على البيانات.
- **القياس الحقيقي وصل إليها وضاع منّا — التسجيل كان «أرسِل وانسَ».** هيا رأت نتيجتها
  الحقيقية (٦ من ٨) على شاشتها بعد جلسة استماع كاملة، ووصل من الثمانية سطرٌ واحد
  فقط لقاعدة البيانات (١٦ أغسطس). السبب: `logAnswer` كانت تُرسل الطلب وتُهمل فشله
  كلّياً — لا إعادة محاولة، ولا أثر يدلّ لاحقاً أن سبعة سطور ضاعت. هذا يخالف مبدأ
  المشروع نفسه («القياس لا الافتراض»): القياس هنا لم يغب، بل فُقد بعد وقوعه. أُصلح
  بطابور إعادة محلّي (`mawhiba_log_queue_v1`): فشل الصفّ الكامل ثم الأساسي معاً
  يُحفَظ الأساسي محلّياً، ويُعاد إرساله عند أوّل فرصة اتصال (بدء التطبيق، أو حدث
  `online`، أو بعد أي تسجيل ناجح لاحق) — لا خوارزمية جديدة، إعادة محاولة بدل إهمال
  نهائي. **وموصولٌ لا مبنيّ من جديد**: `logAnswer`/`logPost` دالّتان مشتركتان بين كل
  قسم في التطبيق، فالإصلاح هنا يشمل الأقسام كلّها بتغييرٍ واحد لا تكراره قسماً قسماً.

### فحص النسخة: ثلاثة أعطال في آليةٍ واحدة — درس ١٨ أغسطس

تشخيص محمد كشف أن **تدريبات B2 المبنيّة له لم تصله ولا مرّة خلال ١٦ ساعة**. والدليل
قاطعٌ لا احتمالي: سجلّه كلّه (١٢ محاولة) خالٍ من `gr_b1p_*`، وجلسته الساعة ٠٩:٠٩ كانت
**عنصرين** والحدّ خمسة — وهذا لا يقع إلا إذا كان بنكه المحمَّل خالياً من الجديد أصلاً.
وثلاثة أعطال متراكبة كانت وراءه:

1. **`BUILD` معلّقٌ على تذكّر إنسان.** الفحص يسأل: هل تحوي الصفحةُ الحيّة وسمَ بنائي؟
   والوسم لم يُحدَّث من ١٧ أغسطس ١٤:٤٥ بينما تغيّر الملفّ **ثماني** مرّات بعده — فنسخته
   القديمة تجد وسمها في الحيّة وتظنّ نفسها محدَّثة. **والعيب نفسه سبق أن أُصلح ثم عاد**،
   وهذا وحده يُثبت أن التذكّر ليس آلية. فأُضيف فحصٌ ثانٍ **تلقائي**: طول النسخة المخزّنة
   (`force-cache`) مقابل الحيّة — يتحرّك مع أيّ تعديل ولا ينتظر أحداً. واتجاه خطئه آمن
   عمداً: تعذّرُ قراءة المخزّنة يُعيد الحيّ فيتطابقان، فيفوت الإنذار ولا يقع كاذباً —
   والكاذب أسوأ لأنه حلقةُ إعادة تحميل تسرق كل لمسة.
2. **الشبكة الاحتياطية كانت تقارن حيّاً بحيّ.** `recheck` يقارن الطول بـ`baseLen`،
   و`baseLen` مأخوذٌ عند التحميل من الجلب **الحيّ** نفسه — فهو يكشف تغيّراً يقع والصفحة
   مفتوحة، ولا يستطيع أن يكشف أن **المعروض** قديم أصلاً. أي أن الشبكتين سقطتا معاً.
3. **وإعادة التحميل كانت تُسقط الهويّة** — والأخطر أنها في `hardReload` أيضاً، وهو
   **الزرّ الذي يُوصى بضغطه عند تعذّر التحديث تحديداً**: `location.pathname+"?_r="`
   يُسقط `?p=mohammed`، فيُنزل محمداً وإلياس في ملفّ هيا بلا أن يشعرا. أي أن علاج
   العطل كان يُبدّل المتعلّم بمتعلّمٍ آخر. صار الرابط يُبنى بـ`reloadUrl()` التي تصون
   الاستعلام ولا تُراكم `_r`، ويُصدَّر بـ`window.__reloadUrl` ليصله الزرّ خارج الغلاف.

**والدرس الأعمّ**: آليةٌ تعتمد على أن يتذكّر أحدٌ خطوةً يدوية في كل شحنة **ستفشل**، ولا
يكفي إصلاحها مرّةً — يجب نزع التذكّر من مسارها. ويحرسها الآن `tests/test_version.js`.

## العمل المتوازي: جلستان على ملفٍّ واحد — درس ١٨ أغسطس

جلستان اشتغلتا على `index.html` في اليوم نفسه، فتعلّمنا ثلاثة أشياء بالثمن:

1. **التعارض يقع في الاسم لا في المنطق، وgit لا يكشفه.** بُنيت ترجمة أزواج
   minimal-pair مرّتين بحقلين (`tr` و`m`) — كلاهما صحيح، فلا تعارض نصّي يُنبّه. حُسم
   بإبقاء `m` (الأسبق إلى `main`) مع الأزواج الثلاثين. **فقبل إضافة حقلٍ لمعنًى قد
   يكون موجوداً، ابحث عن المعنى لا عن الاسم.**
2. **الأعداد المكتوبة في الاختبارات فخّ صامت.** `tests/test_coachonly.js` يُثبّت عدد
   أزرار صفحتَي الأخوين؛ فكل زرٍّ جديد يُفشله بلا أن يكشفه فحصٌ نحوي. (٦ ⇐ ٧ عند
   إضافة زرّ STEP.)
3. **لا تُصلح ما تبني الجلسة الأخرى مساعِداً مشتركاً له.** عيب «النقص» في بانيات
   الجلسات (`build*Plan`: الاحتياط يعمل عند الفراغ التامّ لا عند النقص) موجودٌ في
   **ستّ** بانيات لا خمس — و`buildStepPlan` سادسها. تُرك بلا إصلاحٍ موضعيّ عمداً
   ليَصِله المساعِد المشترك، لا ليُرمى إصلاحان متعارضان.

**والقناة بين الجلستين هي هذا الملفّ والشيفرة** — لا تراسل مباشر بينهما (جُرِّب
`SendMessage` فلم تكن الجلسة الأخرى في متناوله). فما يجب أن تعرفه الجلسة الأخرى
يُكتب هنا أو في تعليقٍ عند موضع الاصطدام نفسه.

### والحاوية نفسها قد ترتدّ — درس ٢٢ أغسطس

مرّتين في يومٍ واحد ارتدّت حاوية التطوير إلى لقطةٍ أقدم (`64c4be1`)، فاختفى من شجرة
العمل كلُّ ما بُني بعدها. **ولم يضع شيء لسببٍ واحد: كان كلّه مدفوعاً إلى الفرع البعيد
أوّلاً بأوّل.** والمرّة الأولى كُشفت قبل الالتزام، والثانية **لم تُكشف إلا برفض دفعٍ
غير سريع التقديم** — أي أن الارتداد لا يُعلن عن نفسه، ولا يُرى إلا حين يصطدم بالبعيد.

فقاعدتان عمليّتان:

1. **قبل أي عمل على الملفّ: `git fetch origin <الفرع> && git status && git log --oneline -3`.**
   إن كان الرأس المحلّي خلف البعيد فالحاوية ارتدّت — يُستعاد بـ`git reset --hard`
   إلى البعيد (أو `git rebase origin/<الفرع>` إن كان في الشجرة عملٌ حقيقيّ لم يُدفَع)
   **قبل** كتابة سطرٍ واحد، لا بعده.
2. **ادفع كل التزامٍ فور اكتماله، لا في نهاية اليوم.** الدفع هنا ليس نشراً بل نسخةٌ
   احتياطية: ما لم يُدفَع لا يوجد. ولا يُدفع دفعاً قسرياً (`--force`) عند رفضٍ غير
   سريع التقديم — الرفض إشارةُ ارتدادٍ تُعالَج بـ`rebase`، لا حالةٌ تُدهَس.

## بوّابة المستوى في بنك القواعد المنسوخ من الكتاب — درس ١٩ أغسطس

سُئل: هل ضعف هيا (A1) في «أخطائي» و«خطتي» سببه نقصٌ منهجيّ أم عطلٌ تقني؟ والفحص كشف
سبباً بنيوياً محدَّداً: كل بنكٍ بُني *بعد* بذرة CEFR (الاستماع/المقروء/دقّة القواعد/STEP)
مُقيَّدٌ بمستواها فعلاً (`listenBankFor`/`readBankFor`/… تُطابق `profileOf().level`)، لكن
**`ENG_ITEMS`** — أقدم بنكٍ في التطبيق، منسوخٌ من كتاب الصف السادس بترتيب وحداته — لم
يكن مقيَّداً بشيء: المبني للمجهول (B1 بمعيار English Profile) في **الوحدة الأولى**، لأن
موضع الدرس في الكتاب لا يدلّ على صعوبته إطلاقاً. فتصل هيا (A1) لمحتوًى فوق مستواها
بدرجتين كاملتين بلا سببٍ غير ترتيب الطباعة.

**العلاج مطبَّقٌ لمبدأ Krashen's i+1 (مدخلٌ مفهوم قليلاً فوق المستوى) لا `مطابقة`**
المستخدَمة في البنوك الأخرى — عمداً مختلفٌ: مستواها ودرجةٌ واحدة أعلى مسموحان
(`ENG_LV_RANK`)، درجتان فأكثر ممنوعتان. التصنيف بمعيار English Profile نفسه، وبنفس
تبويب `GRAM_BANK` حيث تشترك الدروس (لا معياراً ثانياً يتعارض مع الأوّل).

**والقيد على الجديد الحقيقي وحده، لا كل المخزون** — واكتُشف الفرق بالاختبار لا بالحدس
(قسم ١١ في `tests/test_gram.js`): محاولةٌ أولى قيّدت `engPool()` نفسها فكادت تُخفي
عناصر `ITEM_SEED` (u1_g1، u1_g2… أخطاءٌ حقيقية مسجَّلة سابقاً وعليها بُنيت بوّابة السؤال
العالق `itemStuck`/`fixNeeded`) — رُوجعت قبل الشحن. ثم كشف الاختبار نفسه ثغرةً ثانية بعد
الإصلاح الأول: القيد على حلقة `fresh` وحدها لم يكفِ لأن معياره (لا سجلّ SRS) لا يطابق
"رأته فعلاً" — عناصر `ITEM_SEED` الستّة من فئة B1 (u1_g1/u1_g2/u2_g2/u3_g2/u3_g3/u4_g3)
بلا سجلّ SRS رغم كونها أخطاءً حقيقية مسجَّلة في جدولٍ آخر (`itemErrLoad`، أقدم من تتبّع
SRS للعناصر). فأُضيف استثناءٌ: عنصرٌ بسجلّ خطأٍ حقيقي (`itemErrOf`) يتجاوز البوّابة حتى
بلا سجلّ SRS. ومسار إعادة تدريس الدرس الضعيف (`weakRank`/`LESSON_SEED`) بقي خارج البوّابة
عمداً أيضاً — لأنه بُني أصلاً لمراجعة دروسٍ أخطأتها فعلاً (`l_passive` وغيره)، لا لعرض
محتوًى بكرٍ، فقيدُه كقيد `itemErrOf` بالضبط لكن على مستوى الدرس لا العنصر.

**التدقيق الشامل** («طبّق الآلية على كل الأقسام لكل الثلاثة») غطّى كل بنك في التطبيق:
- `LISTEN_BANK`/`READ_BANK`/`GRAM_BANK`/`STEP_BANK`/`MINPAIR_BANK`/`WRITE_BANK`
  مُقيَّدةٌ بمستوى الطالب فعلاً منذ بنائها — لا تغيير مطلوب.
- `ENG_BUILD` مبنيّةٌ أصلاً بحسب `profileOf().level` (`engBuildFor`) — لا تغيير.
- `QUANT_GENS`/`FLEX_GENS`/`READING_ITEMS`/`SPEAK_ITEMS`/`DICTATION_A1`/مواضيع التقوية
  محتوًى أحاديّ المستوى بطبيعته (لا تدرّج CEFR فيه أصلاً) — لا تُقاس بهذا المعيار.
- `ENG_ITEMS` وحده كان الثغرة — أُصلح كما فُصِّل أعلاه.
- ومحمد/إلياس لا يريان قسم «أخطائي»/خطة الإنجليزي هذا إطلاقاً (`only:"coach"` في
  `PROFILES`، ثمانية أزرارٍ فقط على صفحتيهما مؤكَّدةٌ بـ`test_coachonly.js`، بعد أن
  صار سبعةً بزرّ الفيديو التعليمي أدناه) — فالإصلاح عملياً يخصّ هيا وحدها، رغم أن
  التدقيق شمل الثلاثة كما طُلب.

**لم يُقَس أثره بعد**: هل توقّف ظهور B1 بكرٍ فعلاً في جلساتها القادمة، وهل معدّل إتقانها
في الدروس ضمن i+1 يتحسّن. يُحكَم عليه بسجلّ `mawhiba_answer_log` (`domain='engplan'`)
لا يُدَّعى الآن.

## فيديو تعليمي: قصّة متحرّكة بدل فيديو حقيقي — درس ١٩ أغسطس

طلب صاحب المشروع فيديو للثلاثة كلٌ حسب مستواه، بعد أن استعرض قائمة آليات تعليم لغة
عالمية (Duolingo، ELSA، FluentU، Netflix+Language Reactor…). والجواب الصادق قبل البناء:
أغلب القائمة موجودٌ أصلاً بأسماءٍ مختلفة — FSRS-6 هو "التعلّم التكيّفي"، وAzure phoneme
assessment هو "التعرّف على النطق" (أدقّ من ELSA لأنه فونيميّ لا كلميّ)، وLanguageTool هو
"تصحيح الكتابة الآلي"، والشريك هو "معلّم AI". الفجوة الوحيدة الحقيقية: الفيديو.

**ولا فيديو حقيقي هنا** — نفس جدار الترخيص الذي وثّقناه مع `LISTEN_BANK` (١٥-١٦ أغسطس):
لا بنك فيديو مفتوح الرخصة يصلح، ويوتيوب/نتفلكس لا يُعاد توزيعهما، وأصعب من الصوت لا أسهل.
فالحلّ نفس حلّ الصوت بالضبط: نؤلّف المحتوى بأنفسنا كما تؤلّف كل جهةٍ معياريّة موادّها،
ويُعرض كمشاهد متتابعة (رمزٌ كبير متحرّك + جملة) بدل لقطاتٍ مصوَّرة — أمانةٌ في العرض لا
فيديو مموَّه، تماماً كما حلّ النصّ محلّ الصورة في `WRITE_BANK` سابقاً. والسرد صوتٌ حقيقي
عبر `speakEnglish` نفسها — لا اختراع صوتي جديد.

**موصولٌ لا مبنيّ من جديد**: `VIDEO_BANK` بمستويات (٦ قصص/مستوى، ٣-٤ مشاهد للقصّة)، نفس
`fsrsUpdate` للتباعد (`VIDEO_SRS_KEY`)، ونفس `topUpPlan` للاحتياط عند النقص (درس بنك
الاستماع ١٥ أغسطس، مطبَّقٌ من أوّل يوم لا بعد شكوى)، ونفس صيغة سؤال الفهم بعد المحتوى
تماماً كـ`LISTEN_BANK`/`READ_BANK` (Cambridge-style) — فرقها الوحيد أن السرد يتقدّم
مشهداً مشهداً بدل جملةٍ واحدة أو نصٍّ كامل دفعة واحدة، وهذا من طبيعة الفيديو (تتابعٌ
زمني) لا اجتهاداً. الحدّ الأدنى للجلسة ٥ (قاعدة صاحب المشروع ١٨ أغسطس: لا تمرين تحت ٥).

- كل مستوى يرى بنك مستواه هو بالضبط (`videoBankFor`، مطابقة لا i+1 هنا — نفس نمط
  `listenBankFor`/`readBankFor`)، فمحمد B1 يرى قصصاً فيها منعطفٌ ونتيجة، وهيا A1 ترى
  يوماً عادياً بجملٍ قصيرة.
- **لا توليدٌ آليّ بعد** (خلاف الاستماع/المقروء/الكتابة) — البنك مؤلَّفٌ يدوياً كاملاً
  هذه الشحنة الأولى، توفيراً لوقت تعديل الخادم (`supabase-functions-tutor.ts`) وإعادة
  نشره، ولأن سقف الأمان لا يخوض هنا بعد. إن استُنفد البنك بجلساتٍ متكرّرة كما وقع مع
  الاستماع (١٥ أغسطس) فالحلّ نفسه: وضعٌ خامس (`domain:"video"`) في دالّة `tutor`.
- **اختُبر قبل الشحن**: `tests/test_video.js` (تسلسل المشاهد، السؤال بعد آخر مشهد،
  التسجيل، التباعد، النقص لا الخواء، جلسة كاملة بنقرات حقيقية، الظهور على الصفحات
  الثلاث). و`tests/test_coachonly.js` عُدِّل (سبعة ⇐ ثمانية أزرار) — نفس درس «الأعداد
  المكتوبة في الاختبارات فخّ صامت» (١٨ أغسطس) طُبِّق فوراً بدل انتظار فشلٍ يكشفه.

**لم يُقَس أثره بعد**: هل الشكل المتحرّك المؤلَّف يُفهَم كالفيديو الحقيقي أم يحتاج
تحسيناً بصرياً، وهل التوليد الآلي مستحقٌّ لاحقاً أم يكفي البنك الحالي. يُحكَم عليه بدقّة
`domain='video'` وتكرار `q_text` عبر الجلسات القادمة، لا يُدَّعى الآن.

## شكوى إلياس «الأسئلة متكرّرة» — أين فعلاً، وكم — درس ٢٠ أغسطس

اشتكى إلياس تكرار الأسئلة مرّة قبل (١٧ أغسطس، عن `minpair`، فوُسِّع بنكه ٥⇐١٠). عادت
الشكوى، فقيست هذه المرّة **لكل قسم على حدة** بدل التسليم بمصدرٍ واحد — نسبة النصوص
المتمايزة إلى إجمالي الإجابات آخر أربعة أيام (`mawhiba_answer_log`، `إلياس`):

| القسم | إجمالي إجابات | نصوصٌ متمايزة | النسبة | توليدٌ آليّ؟ |
|---|---|---|---|---|
| **`gram`** | ٣٣ | **٥** | **١٥٪** | **لا يوجد** |
| `read` | ٦١ | ٢٦ | ٤٣٪ | يوجد (بطيء) |
| `minpair` | ٧١ | ٣٦ | ٥١٪ | يوجد (بطيء) |
| `step` | ٢٤ | ٨ | ٣٣٪ | لا يوجد |
| `listen` | ٧٥ | ٤٢ | ٥٦٪ | يوجد |
| `video` | ٥ | ٥ | ١٠٠٪ | لا يوجد (شُحن اليوم، عيّنة صغيرة) |

**الأسوأ بمسافة: `gram`.** ٣٣ إجابة على **خمس جملٍ فقط** — كل بنك مستواه بأكمله
(`GRAM_BANK`, `GRAM_N=5`) أُعيد أربع إلى سبع مرّات خلال أربعة أيام، حرفياً نفس الجملة
الأربع خياراتٍ («The party is on Friday at 5 pm.» ٧ مرّات، «How many people came to the
party?» ٧ مرّات...). **والسبب بنيويٌّ لا حظّاً**: `startGram()`/`startStep()` لا يستدعيان
أيّ توليد — خلاف `startListen`/`startRead`/`startWrite`/`startMinpair` التي تستدعي
`genTopUp`/`writeGenTopUp`/`minpairGenTopUp` كلٌّ بعد بدء جلسته. فقسما `gram` و`step`
بقيا ببنكٍ ثابت (٥ وحوالي ٧ لكلّ مستوى) منذ شُحنا (١٧-١٨ أغسطس) بلا نموٍّ إطلاقاً.

**وحتى الأقسام التي فيها توليد لم تكفِ**: `read` (٤٣٪) و`minpair` (٥١٪) كلاهما يستدعي
توليداً، لكن سقفه بطيءٌ جداً مقابل حجم استعمال إلياس — عنصرٌ واحد فقط يُضاف صامتاً بعد
كل جلسة (`genTopUp`) حتى سقف `GEN_BANK_MAX=15`، بينما جلساته المتكرّرة (كما كشف قياس
minpair سابقاً: مرّتان خلال دقيقة) تستهلك من البنك أسرع من نموّه.

**و`video` نظيف حتى الآن لكن العيّنة تافهة** (٥ إجابات، جلسةٌ أولى اليوم، بلا تكرار) —
لا يُحكَم عليه بعد، وبنيته (`VIDEO_N=5` من بنك `٦`/مستوى، **بلا توليد أيضاً**) نفس شكل
`gram` تماماً: جلسةٌ ثانية في نفس اليوم ستكاد تستنفد البنك فتلجأ `topUpPlan` لمراجعةٍ
حرّة تكاد تكون تكراراً تاماً. **مرشّحٌ للتكرار القادم لا شُحن آمناً منه.**

**المقترح** (لم يُنفَّذ — ينتظر الموافقة كسائر ميزات هذا المشروع): وصل `gram` و`step`
و`video` بنفس آلية التوليد الصامت القائمة أصلاً (`genTopUp`/نظيرتها)، ورفع معدّل التوليد
(أكثر من عنصرٍ واحد بعد كل جلسة، أو نداءٌ إضافي عند اكتشاف أن الجلسة ستستهلك أكثر من
نصف البنك) بدل الاكتفاء بعنصرٍ واحد صامت — الأول كافٍ لبنكٍ يُستهلك ببطء، لا لمستخدمٍ
بحجم استعمال إلياس. وكل توليدٍ جديد يمرّ حتماً على نفس تحقّق `parseGenBlock` (لا تلوّث
لغوي، الصواب داخل الخيارات، الشكل الموسوم سليم) — لا يُشحَن توليدٌ بلا تحقّق.

## اختبارات هيا وإلياس — ٢٠ أغسطس

**هيا** (`mawhiba_attempts`، القسم الوحيد الذي يُسجَّل فيه هذا الجدول): science ١٠/١٢،
verbal ١٠/١٢ — الساعة ٠٦:٣٤. وفي `mawhiba_answer_log` نفس اليوم: `video` (بكرٌ اليوم)
٣/٥ (٦٠٪)، `eng_plan/grammar` ٢/١٢ (١٧٪ — أضعف قسم اليوم)، `eng_plan/vocab` ١٢/١٩ (٦٣٪)،
`gram` ٢/٥ (٤٠٪)، `listen` ٥/٨ (٦٣٪)، `minpair` ٦/٨ (٧٥٪)، `step` ١/٦ إجمالاً (ضعيفٌ لكن
عددها صغير)، `pronunciation_a1` ٨/٨ (١٠٠٪). **لم يُفسَّر بعد**: لماذا `eng_plan/grammar`
تحديداً ١٧٪ رغم بوّابة i+1 المشحونة أمس — يحتاج فحصاً منفصلاً لا افتراضاً هنا.

**إلياس**: جلسةٌ قصيرة نظيفة اليوم — `listen` ٤/٤ (١٠٠٪)، `read` ٢/٢ (١٠٠٪)، `video` ٥/٥
(١٠٠٪، أوّل جلسةٍ له في القسم الجديد). بلا `gram`/`step`/`minpair`/محادثة اليوم تحديداً —
شكواه عن التكرار مبنيّةٌ على تراكم أربعة أيام لا اليوم وحده (مفصَّلٌ أعلاه).

## خطة «سلّم الإنجليزية» وفحصها النقدي — درس ٢١ أغسطس

طُلبت خطة إنجليزية شاملة للثلاثة على المعايير الدولية، ثم طُلب فحصها نقدياً قبل اعتمادها.
والفحص كشف **أن التطبيق كلّه تقويمٌ يتنكّر في هيئة منهج** — ستّ مشكلات، أربعٌ عولجت وشُحنت:

1. **نقيس التعرّف لا الإنتاج.** ستّة من تسعة أقسام اختيارٌ من متعدّد، ودرجات CEFR تُعرَّف
   بالإنتاج أساساً. **والدليل حيّ لا افتراضي**: رسالة محمد «مكان أنصح بزيارته» يومَي ١٩
   و٢١ أغسطس متطابقتان تقريباً حرفياً، ودرجته ١٠٠٪ في المرّتين — لأن المقياس الوحيد كان
   عدد الكلمات، فمرّ استرجاعُ نصٍّ محفوظ على أنه إنتاج. **العلاج**: `writeTtr` (التنوّع
   المعجمي) و`writeSelfSim` (تشابه جاكار مع محاولاته السابقة، مخزونٌ محلّي بـ`lsGet` فيُفصَل
   بين المتعلّمين تلقائياً) — يُسجَّلان في `q_text`. و**قياسٌ لا علاج عمداً**: لا يُسقطان
   نجاحاً ولا يُشعلان بوّابة، بنفس نهج `quizErrDelta` — تشابهٌ عالٍ على السؤال نفسه قد يكون
   مشروعاً، فالعتبة تُسمّى من توزيعٍ حقيقي لا تُخترع اليوم.
2. **١٠٠٪ إنذارٌ لا نجاح.** إلياس ٢١ أغسطس: `listen` و`read` و`gram` ١٠٠٪، `video` ٩٠٪.
   وبوّابة i+1 (١٩ أغسطس) تمنع ما هو أصعب من اللازم ولا شيء يرفع الصعوبة حين تكون أسهل —
   **نصفُ حلقةٍ مبنيّ**. **العلاج**: `bankStretched`/`stretchOn` — دقّةٌ ≥٨٥٪ على ≥٨ إجابات
   تفتح بنك المستوى الأعلى **إضافةً** لا استبدالاً (نفس ما يفعله `gramBankFor` أصلاً بـB1+)،
   وتنحلّ تحت ٧٠٪. **بعتبتين لا واحدة** فلا تتذبذب حول عتبةٍ واحدة. والمعيار مسمّى لا مخترَع:
   قاعدة الـ85% (Wilson et al. 2019) ومبدأ الصعوبات المرغوبة (Bjork) — لا رقمٌ اخترتُه.
   **والوصل عند `logAnswer`** وحدها: نقطةٌ مشتركة بين كل الأقسام، فتغييرٌ واحد وصلَ البانيات
   السبع — نفس ما فعله طابور إعادة التسجيل قبله.
3. **تقويمٌ بلا تسلسل تدريس.** FSRS خوارزمية **مراجعة**: تُحسِن جدولة ما دخل النظام ولا
   تقرّر ماذا يدخل ومتى، وEnglish Profile يعطي **قائمة** تراكيب كل درجة ولا يرتّبها. فهيا
   (A1، تحتاج أساساً مرتَّباً) كانت تتلقّى العناصر بترتيب الجدولة لا بترتيب البناء المعرفي.
   **العلاج**: `SEQ_PLAN` — اثنا عشر أسبوعاً لكل مستوى بتدرّج كامبردج، كلٌّ بعبارة «يستطيع
   أن…» (الصيغة المعيارية في CEFR). **وحدُّه معلنٌ صراحةً: إرشادٌ يُعرض لا قيدٌ يُبرمَج** —
   لا يُصفّي بنك أيّ قسم ولا يمسّ التباعد (مُثبَتٌ باختبار)، لأن إخضاع الجدولة له كان
   سيُفسد FSRS المُعايَر (درس «لا تهدم ما هو أصلاً أفضل ممارسة»).
4. **كل KPI معلّقٌ على أن يُسأل.** كل مؤشّر في الخطة كان استعلاماً لا يشغّله أحد تلقائياً —
   **وهذا حرفياً صنف العطل الموثَّق ١٨ أغسطس** («آليةٌ تعتمد على أن يتذكّر أحدٌ خطوةً يدوية
   ستفشل»)، وقد كلّف ١٦ ساعة من تدريبات لم تصل محمداً. **العلاج**: `loadKpi`/`kpiAgg` —
   لوحة قياس داخل التطبيق تقرأ السجلّ مباشرةً (آخر ٧ أيام) وتحسب الدقّة والنصوص المتمايزة
   والعناصر المولَّدة، مع رايات «أسهل من اللازم»/«دقّة منخفضة»/«تكرار». **بلا أداة جديدة**:
   سياسة `anon_select_answerlog` تسمح بالقراءة أصلاً (تُحقِّق منها لا تُفترَض)، ونفس نمط
   `loadProgress` القائم. و`kpiAgg` منفصلةٌ عن الشبكة والعرض فتُختبَر وحدها.

**وثلاثةٌ نُفِّذت بعدها في اليوم نفسه** («نفّذ ما بقي على أعلى المستويات العالمية»):

5. **المحاكاة الأسبوعية** — ورقةٌ من ٢٠ عنصراً بترتيب الورقة الحقيقية (استماع ⇐ قراءة ⇐
   قواعد ⇐ تحليل كتابي)، **بلا تغذيةٍ راجعة حتى النهاية** (الامتحان يقيس ولا يُدرّس، وعرضُ
   الصواب بعد كل سؤال يُفسد استقلال الأسئلة)، بدرجاتٍ بنطاقات كامبردج التقريبية. وثلاثة
   احترازات ضدّ العطل الذي حذّر منه الفحص نفسه: **استبعاد ما أُجيب عنه خلال ٣ أيام**
   (تلوّث إعادة الاختبار) يُقرأ من حقل `last` في مخزون FSRS نفسه لا بسجلٍّ ثانٍ، **ويُرخّى
   تلقائياً** بدل انهيار الورقة مع إعلان عدد المُعاد صراحةً على شاشة النتيجة؛ وتوليدٌ صامت
   يُخصِّب الورقة **القادمة**؛ وخلطُ مواضع الخيارات. **وحدُّها معلن**: لا كتابةَ حرّة فيها —
   تصحيحها يحتاج نداء شبكةٍ لكل نصّ فيكسر التوقيت، وتقديرها بعدد الكلمات وحده كذّبته
   بياناتنا نفسها (محمد ١٠٠٪ على نصٍّ مُعاد).
6. **BKT — معايير عبورٍ مرقّمة** (`bktStep`/`bktMastered`). Corbett & Anderson 1994، وهو
   البديل الذي سمّاه هذا الملفّ نفسه منذ ١٤ أغسطس وصنّفه «لم يُنفَّذ». احتمال إتقانٍ لكل
   مهارة يُحدَّث بقاعدة بايز بعد كل إجابة، **وعتبة ٠٫٩٥ معيارهم المنشور لا رقمٌ اخترتُه**.
   **وp(G) يُشتقّ من عدد الخيارات لا يُخمَّن**: ٥٠٪ في `minpair` (زوجان) و٢٥٪ في `gram`
   (أربعة) — فإصابةٌ في minpair تُرجّح الإتقان أقلّ بكثير، وهذا فرقٌ حقيقي كان يضيع لو
   وُحِّد المعامل. **والوصل عند `logAnswer`** كبوّابة التمدّد قبله.
   **وكشف الاختبار عيباً حقيقياً**: بلغ الاحتمال ٠٫٩٥ بعد **ثلاث** إصابات فقط، فيُعلَن
   «مُتقَن» على كل قواعد A2 بثلاث إجابات. والرياضيات سليمة (ثلاث إصاباتٍ بتخمين ٢٥٪ دليلٌ
   بايزيّ قويّ فعلاً) — **والعيب في خشونة المهارة المُتتبَّعة** (قسم:نوع، لا تركيبٌ بعينه).
   فأُضيف حدٌّ أدنى للأدلّة (`BKT_MIN_N=6`، ممارسةٌ قائمة في نشر BKT) وخُفِّض معدّل التعلّم
   إلى ٠٫٠٨ داخل المدى المنشور — **ولم تُفَبرَك الصيغة نفسها**.
7. **ثراء المفردات — LFP** (`writeLfp`). Laufer & Nation 1995: نسبة ما يكتبه فوق الألف
   كلمة الأكثر شيوعاً (K1)، تُسجَّل مع كل محاولة كتابة. المبتدئ ينتج K1 كلَّه تقريباً
   والتقدّم يظهر بارتفاع النسبة. **وحدُّ القائمة يُقال**: NGSL الرسمية (CC BY-SA 4.0)
   ٢٨٠٠ كلمة لم تُضمَّن كاملةً، والمضمَّن ٥٢٥ كلمة عالية التواتر تقارب K1 — وهي **بياناتٌ
   لا منطق**، فاستبدالها بالرسمية تغييرُ مصفوفةٍ واحدة. وأحجام المفردات المرجعية
   (`VOCAB_TARGET`: ١٠٠٠/٢٠٠٠/٣٢٥٠) **مرجعٌ يُعرض لا رقمٌ يُحسب** — حسابه يحتاج اختبار
   حجم مفرداتٍ منفصلاً (Vocabulary Size Test) لا عيّنة كتابة.

**ودعوى في الفحص تبيّن أنها خاطئة، وتُصحَّح صراحةً**: قلتُ «لا قياس نطق لمحمد وإلياس —
Azure موصولٌ بقسم هيا وحدها». **وهذا خطأ**: `coachHear` تُشغّل تقييم Azure الفونيمي على
كل جملة منطوقة للثلاثة منذ بناء المحادثة. والأرقام من السجلّ: محمد **٨٧** درجة نطق
(متوسّط ٨٨٪)، إلياس **٩٠** (٨٣٪)، هيا **٥٥** (٨٢٪) تحت `domain='coach', qtype='say'`.
لا شيء كان يحتاج بناءً — والدرس: **افحص قبل أن تُدرِج نقصاً في قائمة النواقص**، فالفحص
النقدي نفسه يخضع لقاعدة «القياس لا الافتراض».

**وما يبقى فعلاً**: STEP وB2 مدموجان في تقديرٍ زمنيّ واحد وهما بناءان مختلفان؛ ومهارة BKT
خشنة (تدقيقها يحتاج وسم كل عنصر بمهارته لا بنوعه)؛ ولا أثر مقاساً لأيٍّ ممّا شُحن.

**والوثيقة نفسها**: `https://claude.ai/code/artifact/677e50c4-f89b-4f3a-8068-39532dff9cc1`
(تُحدَّث على نفس الرابط لا تُنشر من جديد). واختبارها `tests/test_plan_v2.js`.

**لم يُقَس أثر أيٍّ من الأربعة بعد** — لا بيانات حيّة بعد الشحن. والحكم عليها: هل تفتح
بوّابة التمدّد فعلاً لإلياس وتهبط دقّته إلى ما دون ١٠٠٪ (وهذا **نجاحٌ** لا فشل)، وهل
يتحرّك `شبه سابق` عند محمد نزولاً، وهل تُفتح لوحة القياس أصلاً.

## ثلاثة أعطالٍ كشفها فحص الجلسات — ٢٢ أغسطس

ثلاثة أسئلة متتالية من صاحب المشروع («كيف اختبارات هيا اليوم؟»، «كيف اختبارات محمد
اليوم؟»، «افحص») كشفت ثلاثة أعطالٍ حقيقية، كلٌّ منها من نوعٍ مختلف — ولم يكن أيٌّ منها
ظاهراً في الدرجات وحدها.

### ١) النقر الأعمى: الدرجة = التخمين العشوائي بالضبط

بيانات هيا: قراءة **٠٫٨ث/٢٥٪**، قواعد **٠٫٩ث/٢٠٪**، استماع **١٫٧ث/٣٨٪** — وأسرع إجابة
**٤٦٥ ملّي ثانية**. وثلاثتها تساوي التخمين من ثلاثة/أربعة خيارات. **وفُرِّق هذا عن الضعف
الحقيقي بمقابلة الزمن الوسيط في جلساتها المنخرطة** (٤٫٨-١٠٫٥ث بحسب القسم) — لا بالحدس.

والعلاج **وصلٌ لا بناء**: نفس البوّابة البنيوية القائمة (`gateStart`/`gateOpen`/`gateLeft`،
التي رفعت الفهم المقروء ١/٣ ⇐ ٣/٣) وُصلت بأقسام الإنجليزية الخمسة عبر `secGateStart` —
الخيارات لا تُعرض قبل زمنٍ أدنى (`SEC_GATE_FLOOR`) يُحسب بطول النصّ ويُسقَّف
(`SEC_GATE_MAX`). **والسقف مقاسٌ لا مختار**: أوّل محاولة حسبت ١٨-٢٥ث لقسم القواعد (جمع
أطوال الجمل الأربع) وهو **فوق** زمنها الوسيط المنخرط ٧٫٨ث — أي أنها كانت ستعاقب اجتهاداً
صادقاً. فسُقِّف كل قسمٍ بزمنه المقاس.

### ٢) العنصر المولَّد المعيب: يُحتسَب خطأً على من أصاب

درجة محمد في STEP ١٧٪. وفحص **نصّ** كل عنصر (لا الدرجة) أظهر أن **٢ من ٣** معيبان:
أحدهما مموّهه «Because the signal was broken the train arrived late.» — ترتيبٌ سليم
تماماً؛ والآخر مموّهه حذفُ فاصلة أكسفورد — ترقيمٌ بريطانيّ صحيح. **فدرجته الحقيقية ٣/٦
لا ١٧٪**، والخطأ في المولّد لا فيه.

والعلاج طبقتان، **والرقابة القبلية عند العميل هي الحاسمة** لأنها تعمل مهما كان النموذج:
- `gjtDefect` في `parseGenPickBlock` يُسقط العنصر قبل أن يصل المتعلّم، بسببٍ مسمّى
  (`clause_fronting`/`oxford_comma`/`not_one_correct`) يُسجَّل في `mawhiba_answer_log`
  (`domain='gen', qtype='reject'`) ومعه نصّ العنصر — فيُقاس معدّل الرفض لاحقاً.
- و`systemGenStep` في الخادم صار يفرض **آليةً فرعية** من `subByTag` (١٣ آلية) كما يُفرض
  `GEN_TOPICS` أصلاً، ويمنع صراحةً بناء العنصر على تفضيلٍ أسلوبيّ تقبله الإنجليزية
  (فاصلة أكسفورد، تقديم الجملة الفرعية، نوع علامات الاقتباس، ‎-ise/-ize). **نُشر نسخة ٢٧.**

**وثلاث حالات كاذبة كشفها اختباري لنفسي** (`tests/test_gjt.js`): تجريدُ الترقيم والكبتلة
في المقارنة جعل فاصلة اللصق (`;` مقابل `,`) وعناصر الكبتلة تُرفَض ظلماً — فصارت مقارنة
أكسفورد حسّاسةً للحالة والترقيم. وعنصرٌ مؤلَّف (`st_b1_1`) رُفض أيضاً لأن مموّهه **يُقحم**
الجملة السببية وسط الجملة (خطأٌ حقيقي) لا **يُقدّمها** (ترتيبٌ سليم) — فُصِل بـ`gjtIsRotation`.

### ٣) بنك الكلمات: محاولةٌ تُحرَق على جملةٍ ناقصة

هيا **٣ من ٦٢** محاولة بناء (٥٪) على تسعة أيام. والمقابلة حسمت أن الأداة تعمل: إلياس
٤/٥ ومحمد ٤/٤. والسجلّ كشف الشكل: «Thank you with helping» لهدفٍ من **ثماني** كلمات —
**ناقصةٌ لا مبعثرة**. والسبب سطرٌ واحد: `wbReady(){… wbPicked.length>0}` — كلمةٌ واحدة
تكفي لتفعيل زرّ التحقّق. أُصلح بـ`wbNeed()`/`wbLeft()`، والزرّ يعرض «بقي ن كلمة» معطَّلاً
بدل أن يصمت.

**ولم يُقَس أثر أيٍّ من الثلاثة** — لا بيانات حيّة بعد الشحن. والحكم: هل يرتفع زمن إجابة
هيا فوق أرضيّة البوّابة وترتفع دقّتها فوق التخمين؛ وكم عنصراً يرفضه `gjtDefect` فعلاً
(`domain='gen'`)؛ وهل تتجاوز نسبة نجاح هيا في البناء ٥٪.

## القاعدة الثالثة تشمل المقاييس لا الميزات وحدها — درس ٢٢ أغسطس

بُني منعُ التوائم في الجلسة بمقياسٍ صنعتُه بيدي: تشابه جاكار على الكلمات المفردة، ومعه
قائمة كلماتٍ شائعة أُسقطها كتبتُها بنفسي. فأطلق بلاغاتٍ كاذبة على النصوص القصيرة، فرقّعتُه
بحدٍّ أدنى للطول (`TWIN_MIN=18`) — رقعةٌ على مقياسٍ رديء لا إصلاحٌ له. وقال صاحب المشروع:
**«البلاغات الكاذبة على النصوص القصيرة لأنك تخترع أكواد وكأنها غير موجودة ومطبقة قبلك»**.

والصيغة المعياريّة اسمها **w-shingles** (Broder 1997، وعليها تقوم كشوف التكرار في محرّكات
البحث): يُقارَن تشابهُ متتالياتٍ من ثلاث كلمات لا كلماتٍ مفردة. وقِيس الاثنان على نصوصهم هم
لا على أمثلةٍ مؤلَّفة:

| الزوج | مقياسي | w-shingles (k=3) | TF-IDF |
|---|---|---|---|
| طبخ ↔ طبخ (توأمٌ حقيقي) | ٠٫١٢ | ٠٫٠٣٤ ✓ | ٠٫٣٦٤ |
| طبخ ↔ كرة قدم | ٠٫٠٣ | ٠ ✓ | ٠٫٢٤٢ |
| صيغتا كتابة (**بلاغٌ كاذب**) | ٠٫٢٧ ✗ | **٠** ✓ | ٠٫٣٧٧ ✗ |
| عنصرا قواعد (**بلاغٌ كاذب**) | ٠٫١٣ ✗ | **٠** ✓ | ٠٫٢١١ |

فاستُبدل المقياس كلّه، **وسقطت معه الرقعةُ وقائمةُ الكلمات** — كلتاهما كانت تعويضاً عن
رداءته لا حاجةً حقيقية. **والدرس**: «لا بناء علاج من الصفر» لا تعني الميزات وحدها، بل
**«ولا مقياساً من الصفر»** — فالمقياس المصنوع باليد يُخطئ بصمت، ويُبنى على خطئه رقعٌ يُثبّته.
وقبل كتابة أي دالّة تشابه أو تسجيلٍ أو عتبة: ابحث عن اسمها في الأدبيات أوّلاً.

### وسجلُّ ما يفعله النظام يُفصَل عن سجلّ ما تفعله هي

كان `planNoTwins` يُسجّل الإسقاط تحت `domain` القسم نفسه (`step`/`listen`…) بصوابٍ `null`،
فصار **أوّل صفٍّ يجده كل من يستعلم `domain='step'`**، ويدخل مقامَ كل حساب دقّةٍ على القسم
بصفٍّ بلا نتيجة. **ولم يكشفه فحصي بل `test_step`** — سطر «إجابته النصّية مسجَّلة» فشل بينما
سطر «سطرٌ وصل الخادم» نجح، وهذا بالضبط شكلُ «صفٌّ آخر سبق الصفَّ المقصود».

والعُرف كان قائماً في المشروع أصلاً ولم أطبّقه: `gjtDefect` وراية LanguageTool تُسجَّلان في
`domain='gen'`. فالقاعدة تُقال صراحةً الآن: **ما يفعله النظامُ بالعنصر** (إسقاطٌ، رفضٌ، راية)
في `domain='gen'`، **وما تفعله هي بالسؤال** في سجلّ القسم. ويُسجَّل ما وقع لا ما نُوِيَ:
العنصر الذي عاد لعدم وجود بديلٍ متمايز لم يُسقَط، فلا يُكتب مُسقَطاً.

### واختبارٌ يُثبّت تصميماً كذّبته البيانات يجب أن يُراجَع لا أن يُدهَس

فشل اختباران بعد شحن اليوم، وكلاهما كان يُثبّت تصميماً صحيحاً **يوم كُتب**:

- `test_planread`: «القواعد بلا بوّابة (لها بوّابة التصحيح الخاصة)». وفُحصت الحجّة قبل
  تغييرها: بوّابة التصحيح (`fixOn`/`itemStuck`) إعادةُ تعليمٍ **بعد** تكرار الخطأ، لا بوّابةَ
  تسرّعٍ **قبل** عرض الخيارات — غرضان يجتمعان ولا يتنازعان، والأولى لا تمنع نقراً أعمى
  أصلاً. وكذّبت البيانات الحجّة: هيا ٠٫٩ث/٢٠٪ في القواعد = التخمين من أربعة بالضبط.
- `test_practice`: `gates.length===4`. والعددُ وحده كان يحمل المعنى («لا عائلة −١ بعد»)،
  فتكسره كلُّ إضافةٍ مشروعة ولا يقول أيّ اسمٍ تغيّر — **درس «الأعداد المكتوبة في الاختبارات
  فخّ صامت» (١٨ أغسطس) واقعاً مرّةً أخرى**. فصار الجدول مُثبَّتاً بأسمائه لا بعدده.

## إحصاء ١٠٢: مقرّرٌ جامعي بعينه لمحمد وحده — ٢٥ أغسطس

طلب صاحب المشروع مع ستّة مرفقات: «افتح ايقونة جديدة في صفحة محمد باسم إحصاء ١٠٢ وحط له
الاسئلة المرفقة… واللي في الملفات وما له أسئلة، انشئ اسئلة». والمرفقات من مقرّر «أحص ١٠٢»
بجامعة الملك سعود (عمادة السنة الأولى المشتركة): ورقة أسئلةٍ بخطّ اليد (١٠ أسئلة)، واختبارٌ
نهائي محلول (٢٠ اختياراً من متعدّد + ٧ مقالية، الفصل الأول ١٤٣٨-١٤٣٩)، وملخّصٌ ٥٥ صفحة،
وملزمةٌ ٩٢ صفحة، وورقتا مفاهيم (المجموعات وتعريفات الإحصاء / المتغيّر العشوائي والتوزيع الثنائي).

**وأربعةٌ من الستّة صورٌ ممسوحة لا نصّ** (`Scanned by CamScanner`) — `pymupdf` يستخرج منها صفراً.
فقُرئت **بصرياً** صفحةً صفحة (تحويلها إلى صور ثم قراءتها) لا بـOCR: الأسئلة بخطّ اليد،
وOCR عربيّ على خطّ يدٍ يُخطئ الأرقام تحديداً — وخطأ رقمٍ واحد في مفتاح إجابة أسوأ من غياب
السؤال كلّه. `pdftoppm` غير مثبّت في هذه البيئة، فالتحويل بـ`pymupdf` نفسها.

**موصولٌ لا مبنيّ من جديد**: نفس بنية `STEP_BANK` حرفياً — بنكٌ موسوم + `fsrsUpdate` للتباعد
+ `topUpPlan` عند النقص + شرحٌ لكل خيارٍ بعد القفل + خلط مواضع الخيارات كل جولة + شاشة نتيجة.
لا خوارزمية جديدة ولا مكوّن واجهة جديد.

**والفرق الوحيد من طبيعة المحتوى لا اجتهاداً**: مقرّرٌ عربيّ في الرياضيات لا لغةٌ إنجليزية،
فلا `lv` ولا CEFR — التبويب بالفصل (`ch`، ثلاثة عشر فصلاً) كما يبوّبه المقرّر نفسه. وأُضيف
`sol` (الحلّ خطوةً خطوة) فوق شرح كل خيار، لأن مسألةً حسابية لا يكفيها «لماذا هذا الخيار خطأ»
بلا إظهار الطريقة — امتدادٌ لمبدأ «الشرح منهجٌ لا جواب» المعتمد في عناصر ترتيب STEP.

**ومصدر كل عنصرٍ مسجَّل** (`src` = «ملف» أو «مؤلَّف») ويدخل السجلّ في `q_text`: ٦٦ منقولةٌ من
المرفقات و٢٣ مؤلَّفةٌ لمحتوًى وردَ فيها بلا سؤالٍ عليه — ومنها ستّة لتدريبٍ نصَّ الملفّ على
أنه **«تدريب غير محلول»** (حجر النرد، X العدد الظاهر: E(X)=3.5، V(X)=2.92)، ومفهوما «الإرفاق»
و«تحقّق العشوائية» الواردان شرحاً بلا سؤال. فيُعرف لاحقاً بالاستعلام أيّ الصنفين أنفع بلا تخمين.

**ولا توليدٌ آليّ هنا قطعاً** — خلاف الاستماع/المقروء/الكتابة/القواعد/STEP. مولّد `tutor`
نموذجُ لغةٍ لا حاسبة، ورقابةُ صحّة مسألةٍ محلولة أصعب من أن تُتحقَّق آلياً: نفس قرار
`MINPAIR_BANK` (١٧ أغسطس) بامتناع النموذج عمّا لا يُرقَب. وخطر التكرار الذي أوجب التوليد
هناك لا يقوم هنا: ٨٩ عنصراً والجلسة ثمانية.

- **الحساب فُحص برمجياً لا بالنظر** قبل الشحن: كل جواب رقمي أُعيد اشتقاقه بـnode مستقلّاً
  (توافيق وتباديل، ربيعيات ومئينات بصيغة الرتبة، تباين العيّنة على n−1، بيز، ذو الحدين)
  وقُوبل بما في البنك. **وكشف الفحص ثلاثة**: خلطٌ منّي بين مراكز الفئات والتكرار المتجمّع
  الصاعد في نصّ معطياتٍ واحد، و`0.2343` في ورقة الحلّ نفسها وهي **قصٌّ للرقم لا تقريب**
  (15÷64 = 0.234375 ⇐ 0.2344 — صُحِّح ونُصَّ على الفرق في الحلّ)، وفرقُ رمزٍ لا قيمة.
- **`Var(2−3X)` سالبةً في ورقة الطالب** (`−13.32`) — والتباين لا يكون سالباً أبداً. أُبقيت
  الصيغة السالبة **مموّهاً** مع شرحٍ يسمّي الآلية (نسيان تربيع المعامل)، بدل تصحيحها بصمت:
  نفس مبدأ «شكل الخطأ لا وقوعه» — يُعلَّم الخطأ الشائع لا يُخفى.
- **الظهور لمحمد وحده** (`p.id==="mohammed"` في `renderHomeCoachOnly`) — الطلب نصَّ «في صفحة
  محمد»، وهو المقرّر الجامعي لا مهارةٌ عامّة تُعمَّم كالاستماع والقراءة. فصار عدد أزراره
  **تسعة** وإلياس **ثمانية** — أوّل مرّة تفترق الصفحتان. و`test_coachonly.js` عُدِّل فوراً
  (درس «الأعداد المكتوبة في الاختبارات فخّ صامت»، ١٨ أغسطس) ويحرس الآن الافتراق نفسه:
  ظهوره عند محمد وغيابه عند إلياس وهيا.
- **قائمة فصولٍ قبل الجلسة** (`renderStatMenu`): جلسةٌ شاملة تخلط المقرّر كما يخلطه النهائي،
  أو مراجعة فصلٍ بعينه — وهذا كيف يذاكر طالبٌ لاختبار، لا ترفٌ. وكل فصلٍ فيه ثلاثة أسئلة
  فأكثر (يحرسه الاختبار) فمراجعته تعطي جلسةً لا سؤالاً.
- **اختُبر قبل الشحن**: `tests/test_stat.js` (٣٢ فحصاً: سلامة كل عنصرٍ في البنك، الظهور
  والغياب في الصفحات الثلاث، عشر جلساتٍ متتالية بلا تكرار، حصر جلسة الفصل في فصله، التباعد،
  التسجيل بنصّ السؤال والخيارات وموضع الصواب والمصدر، جلسةٌ كاملة بنقرات حقيقية).

### واتّجاه النصّ عطلٌ يشحن حلولاً مقلوبة بلا أن يُسقط اختباراً — درس ٢٥ أغسطس

الاختبارات الاثنان والثلاثون نجحت كلّها، **ثم أظهرت أوّل لقطة شاشة أن الحلّ يُعرض مقلوباً**:
`= 736 ÷ 9 = 81.78` يظهر `= 81.78 = 9 ÷ 736`. والسبب أن السطر **بلا حرفٍ قويّ إطلاقاً**
(أرقامٌ ورموزٌ فقط)، فيرث اتّجاه الفقرة العربية من حوله. و**٦٧ سطراً من ٢٨٧** في البنك على
هذه الصفة — ربع الحلول. ولا اختبارٌ منطقيّ يمسك هذا: النصّ في DOM صحيحٌ تماماً، والانقلاب
يقع في **العرض** وحده.

- **و`unicode-bidi:plaintext` لم يكفِ** رغم أنه الأداة المصمَّمة لهذا — فجُرِّبت ورُئي أثرها.
- **والمحاولة الثانية أفسدت ما كان سليماً**: عزلتُ كل مقطعٍ رياضي داخل السطر في `span`، فابتلع
  العزلُ علاماتِ الترقيم المحايدة المجاورة (النقطتين والأقواس) فصار «مقرّرات: M = 82 (وزن ٣)»
  يُعرض «مقرّرات) M = 82 وزن: ٣». **علاجٌ أوسع من الداء يصنع داءً ثانياً** — والدليل عليه لقطة
  شاشةٍ ثانية لا استنتاج.
- **فالقاعدة الآمنة على مستوى السطر لا داخله**: بلا عربية ⇐ من اليسار، وفيه عربية ⇐ من اليمين.
  والمعادلات الطويلة (٨ منها) نُقلت إلى أسطرٍ مستقلّة **في المحتوى** لا في العرض — فهي كانت
  تنكسر في منتصفها عند الالتفاف (`… = 246 +` ثم `130 + 360 = 736`) فتُقرأ حدودها بترتيبٍ خاطئ.
- **وفخٌّ ثالث في نطاق المحارف نفسه**: كتلة العربية `0600–06FF` تضمّ علامة النسبة `٪` (U+066A)
  والفاصلة والأرقام الهندية — وكلّها **محايدةٌ أو ضعيفة** لا حروفاً قويّة. فالمطابقة على الكتلة
  كاملةً كانت ستقلب سطراً كـ`= 0.527 × 100٪`. النطاق الآن حروفٌ قويّة وحدها.
- **ويحرسه الاختبار الآن** (`test_stat.js` قسم ٩): كل سطرٍ في البنك يُقابَل اتّجاهُه المحسوب
  بالمتوقَّع، والحالات الأربع التي انكسرت فعلاً مثبَّتةٌ بأعيانها، ويُتحقَّق من DOM الحيّ لا من
  الدالّة وحدها.

**والدرس الأعمّ**: اختبارٌ يقرأ النصّ لا يرى الاتّجاه. فأيّ قسمٍ يخلط العربية بالرياضيات أو
بالإنجليزية **يُنظَر إليه بلقطة شاشة قبل الشحن**، ولا يُكتفى بنجاح الاختبارات.

**لم يُقَس أثره بعد**: دقّة كل فصلٍ على حدة (`domain='stat'` و`qtype` = اسم الفصل)، وهل
المؤلَّف يختلف عن المنقول من الملفّ في الصعوبة (`q_text` يحمل الوسم). ولا يُدَّعى أنه يرفع
درجته في المقرّر حتى تُقاس درجةٌ حقيقية.


## الامتناع المبنيّ على تقديرٍ لم يُقَس — درس ٢٥ أغسطس

بقي `supabase-functions-tutor.ts` **أربعة أيام** مكتوباً بنسخة ٢٨ في المستودع ومنشوراً
بنسخة ٢٧، وكتبتُ هنا سبباً بدا وجيهاً: أداة النشر المتاحة لا تقبل إلا نصّ الملفّ كاملاً
في نداءٍ واحد، فالنشر «إعادةُ كتابة ٨٩٢ سطراً بيدي» لخادمٍ يخدم محادثة محمد وإلياس،
وأيّ انحرافٍ في النسخ يكسر محادثتهما. فأُحيل إلى جهاز صاحب المشروع (`supabase functions
deploy tutor`). فقال: **«سوه انت»**.

**والتقدير الذي بُني عليه الامتناع كان خاطئاً، وقياسه استغرق ثوانيَ لم أُنفقها.** قِيس
الملفّ فعلاً: ٩٣٧ سطراً، ٦٦٧٧٥ بايتاً، ٥٤٨٨٣ محرفاً منها ١١٦٠٣ عربية — أي نحو **١٩ ألف
رمز** لا ٣٣ ألفاً كما قدّرتُ. وأصل المبالغة أنّي قدّرت من ناتج أداة القراءة الذي يحمل
أرقام الأسطر وتنسيقها، لا من الملفّ نفسه. ونُشر في نداءٍ واحد فعلاً (نسخة ٢٨، ACTIVE).

**وقاعدة «الاختبار قبل الشحن» لم تُنقَض بل طُبِّقت في موضعها الصحيح**: التحقّق الممكن هنا
ليس نداءَ الدالّة الحيّة (محجوبٌ بسياسة الشبكة، موثَّقٌ أعلاه) بل **فحص النصّ المرسَل قبل
إرساله** — وقد كشف فعلاً عيباً حقيقياً في تعديلي السابق: `GEN_CULTURE` كان مُقحَماً بين
شطري جملةٍ واحدة في `systemGenGram` و`systemGen` («Do NOT reuse a» ⇐ القيود ⇐ «stock
textbook example») فتنكسر التعليمة التي يقرؤها النموذج. فُحصت المواضع الستّة كلّها آلياً
وأُصلح الشطران قبل النشر.

**والدرس**: الامتناع عن عملٍ مطلوب بحجّة كلفته يوجب **قياس** الكلفة أوّلاً — وإلا فهو
افتراضٌ لا حكم، وهذا نفس ما ينهى عنه المشروع في كل موضع («القياس لا الافتراض»). وقد كلّف
هذا الافتراضُ أربعةَ أيام بقي فيها المولَّد أجنبيَّ الأسماء والأماكن بينما البنك المؤلَّف
عربيّ — تناقضٌ يراه الأبناء ولا نراه في السجلّ.

## أربعة أعطالٍ كشفها فحص جلسات ٢٥ أغسطس — والخامس أن التعريب لا يرتدّ

سُئل «كيف اختبارات الثلاثة اليوم بالتفصيل؟». والدرجات وحدها لم تكن تكشف شيئاً من هذا.

### ١) مسطرتان مختلفتان على المتعلّمة نفسها في الدقيقة نفسها

هيا في `pronunciation_a1` (كلمات مفردة) ٧٨-٩٨٪ و`engine='azure'`، وبعدها بدقيقتين في
`speaking` (جمل) ١٣٪ و`engine` **فارغٌ في الثمانية كلّها**. والسبب أن `speakFinish` كان
ينادي `PRON_GROQ_FN_URL` وحده: الكلمة تُقاس بالفونيمات والجملة بنصّ التفريغ.

**وسجلّ الكلمات الجديد (٢٣ أغسطس) هو ما جعل هذا مرئيّاً**: `pass`⇐«base» و`That wasn't`
⇐«They went» — أي /p/⇐/b/ و/ð/⇐/d/، أشهر خطأين موثَّقين لمتعلّمي العربية. ونصُّ Whisper
لا يفصل «نطقتها خطأً» عن «سمعها المفرّغ خطأً»، والفونيمات تفصلهما.

**وهذا لا ينقض قرار «لا prompt»**: ذاك عن تلقين Whisper الجملةَ المستهدفة فيُعيدها نصّاً
فيقع «نجاح كاذب». وAzure يُصحّح الصوت على نصٍّ مرجعي — يُعطي صفراً لمن لم ينطق، فلا يمكن
أن يُصدّق نجاحاً لم يقع. و`azureTry(target)` عامّةٌ تقبل أيّ نصّ مرجعي أصلاً: وُصلت، ولم
تُبنَ. ونفس السقوط إلى Groq عند التعثّر، **وسببُ السقوط يدخل السطر الآن** (`azure_fail`)
كي لا يعود «الحقل فارغ» غامضاً كما كان اليوم.

### ٢) `ENG_BUILD`: بلا طبقة A1 أصلاً، وبوسومٍ تناقض معيار المشروع نفسه

هيا ٠ من ١١ في `eng_plan/build1`. **وإصلاح ٢٢ أغسطس نجح فعلاً** — الإحدى عشرة كلّها
استعملت مجموعة الكلمات كاملةً، فلم تعد تُرسل جملةً ناقصة. لكن نسبتها لم ترتفع: انتقل
العيب من «ناقصة» إلى «مبعثرة».

**والسبب جزئياً في البنك**: `engBuildFor("A1")` كانت تُعيد عناصر `lv:"A2"`، و`ENG_BUILD`
**٢٤ عنصراً صفرٌ منها A1** — فهيا لم ترَ جملةً في مستواها قطّ. وأسوأ: **خمسةٌ موسومةٌ A2
ودروسها مصنَّفةٌ B1 في `ENG_LESSON_LV` نفسه** (الموصول، المبني للمجهول، الشرط، المضارع
التامّ ×٢)، وثلاثةٌ منها عُرضت عليها اليوم.

**وهذا نفس عطل ١٩ أغسطس في `ENG_ITEMS` يتكرّر في بنكٍ أعلنّاه سليماً** — كُتب هنا حرفياً
«`ENG_BUILD` مبنيّةٌ أصلاً بحسب المستوى — لا تغيير»، وكان التدقيق ناقصاً: الدالّة موجودة
لكنها بلا طبقة A1 وتثق بوسمٍ يناقض المعيار.

**والعلاج بنيويّ لا تصحيحُ بيانات**: أُضيفت عشرة عناصر A1 (مضارع بسيط، ٤-٧ كلمات، محتوًى
سعودي)، ورُفعت الخمسة إلى B1، **و`engBuildLv` تأخذ الأعلى بين الوسم ودرجة الدرس** — فوسمٌ
خاطئ في عنصرٍ يُضاف غداً لا يسرّب شيئاً. والقيد i+1 نفسه المعتمَد في `engLevelOk`.
**و`ENG_LV_RANK` كانت بلا `B2`** — وهو ثقبٌ فتحه رفعُ محمد إلى B2 بنفسي: `rank[undefined]+1`
يُنتج `NaN` فيُحجب كلُّ شيء عن طالب B2. أُغلق.

### ٣) `step` خارج بوّابة التسرّع

`secGateStart` موصولةٌ بستّة أقسام و`step` ليس منها. وهيا اليوم ٠ من ٦، ومنها عنصرُ حروفٍ
كبيرة بأربع جملٍ كاملة أُجيب في **١٫٣٧ ثانية**. وُصل — **بأرضيّةٍ لكل نوعٍ لا واحدة**، لأن
الأزمنة المقاسة تُفرّق الأنواع فعلاً: `pick` ٢١٫٠/١٨٫١ث و`gap` ٤٫٠/١٢٫١ث و`order` ١٦٫١/١٨٫٠ث
(وسيطا المنخرطَين). والقاعدة كما وُضعت أصلاً: نصف **أدنى** وسيطٍ منخرط، فلا تُحبَس أسرعُ
إجابةٍ صادقة. و`pick` مطابقٌ عمداً لشكل `gram` فأخذ أرقامه المُجرَّبة لا رقماً جديداً.

### ٤) `gram` و`step` يُسجّلان ترتيب البنك لا ترتيب العرض

`gramSetupRound`/`stepSetupRound` يخلطان العرض فعلاً، لكن `q_text` كان يُبنى من `it.c`
بترتيبه الأصلي. فظهر الصواب في الموضع «٢» في خمسة عناصر متتالية لهيا (٠ من ٥) — **لأن هكذا
كُتب البنك لا لأنها رأته هناك**. أي أن انحياز الموضع كان غير قابلٍ للقياس في القسم الذي
نالت فيه صفراً، وهو نقضٌ لعُرف المشروع: «تُسجَّل بموضعها لا بشيفرتها». صار السطر بترتيب
العرض، والإجابة تحمل موضعها المعروض.

### ٥) والتعريب لا يُنظّف ما وُلِّد قبله

عناصرُ «Leo/Sam/Tom» ظهرت في جلسات اليوم فعلاً. وفكُّ طوابعها الزمنية من معرّفاتها
(base36) أثبت أنها مولَّدةٌ ٢٠-٢٢ أغسطس — **قبل نشر نسخة ٢٨ بأيّام**، فلا تُكذّبها.
لكنها تكشف أن قيود الخادم لا تمسّ ما استقرّ في البنوك المحلّية على أجهزتهم.

**والآلية قائمةٌ في المشروع أصلاً ولم أخترع غيرها**: رفعُ لاحقة المفتاح يهجر المخزَّن
الملوَّث (فُعل مرّتين من قبل: `_v2` للتلوّث العربي و`_v3` للأسئلة العربية). رُفعت السبعة
كلّها معاً — استماع وقراءة وكتابة وقواعد وSTEP وفيديو وجمل أزواج الاستماع.

**ولا شيء من هذا مقاسُ الأثر بعد.** والحكم: هل يعود `engine='azure'` في صفوف `speaking`
وتنكشف أصواتها الضعيفة؛ وهل ترتفع نسبة هيا في `build1` فوق الصفر بعد أن صارت ترى A1؛ وهل
يتجاوز زمنها في `step` أرضيّة البوّابة؛ وهل يظهر انحيازُ موضعٍ حين صار قابلاً للقياس.

**وواحدٌ لم يُفسَّر ولا أدّعي فيه شيئاً**: `verbal` و`science` عند هيا اليوم ٨٣٪ بزمنٍ
وسيط ١٫٠-١٫٣ ثانية. أعلى بكثير من التخمين فليست نقراً أعمى، وأسرع من قراءة السؤال. لا أعرف
السبب.

**وشكل خطأ إلياس في `minpair` نظيفٌ ويستحقّ متابعة**: ٥ من ١٠ (التخمين بالضبط من خيارين)،
**وأخطاؤه الخمسة كلّها في اتّجاهٍ واحد** — `sit`⇐seat، `cost`⇐coast، `pull`⇐pool،
`wet`⇐wait، `bought`⇐boat: في كل مرّة اختار العضو الطويل/المشدود. والعربية بثلاث حركاتٍ
بتقابل طول، بلا /ɪ/ /ʊ/ /e/ مستقلّةً عن /iː/ /uː/ /eɪ/ — وهذا الشكل موثَّقٌ في إدراك
متعلّمي العربية. لا علاج بعد: يُقاس على جلساتٍ أخرى قبل أن يُبنى له شيء.

## نظام منع التكرار — والقياس كذّب الفرضية — ٢٥ أغسطس

طُلب نظامٌ يمنع تكرار الأسئلة، بفرضيّةٍ أن البنوك فيها أسئلةٌ متشابهة. **والقياس كذّبها.**

### ما قِيس فعلاً (لا ما افتُرض)

**التشابه النصّي داخل البنوك: صفر.** `tests/bank_dupes.js` بـw-shingles (k=3، Broder 1997)
على **أربعة عشر بنكاً** و**١٣٤٨٣ مقارنة**، والمقارنة داخل المستوى الواحد فقط (لأن «نفس
المفهوم بمستوى أصعب ليس تكراراً» — قاعدة صاحب المشروع). أعلى تشابهٍ وُجد **٠٫٣٩٣** ولم
يبلغ عتبة القريب (٠٫٥) **ولا مرّة واحدة**. فلا بنك يقترب من عتبة الـ١٥٪.

**والاختبار يتحقّق من نفسه**: بنكٌ لا يُستخرَج نصّه يُعطي «صفر تكرار» وهو لم يقس شيئاً.
فيُعرض متوسّط الكلمات وعدد العناصر الفارغة صراحةً — وكشف هذا حدّاً حقيقياً: `DICTATION_A1`
عناصره **كلمةٌ واحدة**، وk=3 لا ينطبق عليها أصلاً، فصفرُه لا معنى له ويُقال ذلك.

**وتكرار العرض: ١٧٪-٨٣٪** — وهو الشكوى الحقيقية، ولا يقيسه أيّ مقياس تشابهٍ نصّي لأن
العنصر **مطابقٌ لنفسه لا شبيهٌ بغيره**. آخر سبعة أيام:

| | العروض | العناصر | التكرار | أسوأ عنصر |
|---|---|---|---|---|
| إلياس · `minpair` | ٨٥ | ١٧ | ٨٠٪ | **١٠ مرّات** |
| هيا · `eng_plan` | ٤٣٩ | ١٠٦ | ٧٦٪ | **٤٤ مرّة** |
| هيا · `gram` | ٢٥ | ٥ | ٨٠٪ | ٥ |
| محمد · `stat` | ١٢ | ١٢ | **٠٪** | ١ |

**والتكرار في اليوم الواحد** (لا يجدوله FSRS أبداً، فهو عرَضيٌّ قطعاً): هيا ١٩٪،
إلياس ١٨٪، محمد ٤٪.

### السبب البنيوي، والعلاج

`topUpPlan` — وهي المشتركة بين البانيات التسع — كانت تملأ الجلسة **عشوائياً** من البنك
حين يفرغ المستحقّ، فيعود ما عُرض قبل ساعة بنفس احتمال ما لم يُعرض قطّ.

**العلاج ترتيبٌ لا إقصاء**: ما لم يُعرض قطّ ⇐ الأقدم عهداً ⇐ المحجوب، وما عُرض اليوم
أخيراً دائماً. فتبقى قاعدة «الجلسة لا تنقص عن حدّها» (١٨ أغسطس) بحرفها.
والتهدئة: **٧ أيام** للمُصاب و**٣** للمُخطَأ.

- `seenRecord` عند **`logAnswer` وحدها** — نقطةٌ يمرّ بها كل قسم، فتغييرٌ واحد خدم
  البانيات التسع. والفصل بين الأبناء من `pkey` القائمة لا من شيفرةٍ جديدة.
- `planSeenReport` يُسجّل الاستنفاد في `domain='gen'` باسم البنك وعدد المحجوب والباقي
  بكراً — فيُعرف بالرقم لا بالشكوى.
- `seenSeed` يبذر السجلّ من الخادم عند الإقلاع (**دمجٌ لا استبدال**)، وفشلُه لا يُعطّل شيئاً.

**و`question_history` رؤيةٌ لا جدولٌ ثانٍ**: أعمدتها كلّها موجودة في `mawhiba_answer_log`
أصلاً، وجدولٌ ثانٍ يعني كتابةً مزدوجة ومصدرَي حقيقةٍ يتباعدان — وهذا نقضٌ لقاعدة «لا تبنِ
من الصفر ما يمكن تعديله». وبـ`security_invoker=true` حتى تسري RLS بدل تجاوزها
(الافتراضي صلاحيةُ المالك، وهو تجاوزٌ صامت — Broken Access Control في OWASP Top 10).

### ما لم يُنفَّذ كما طُلب، ولماذا

**١) «لا يظهر السؤال إذا أجاب عليه صح من قبل» يناقض التعلّم المتباعد نفسه.** جوهر SRS
إعادةُ ما أُصيب بفواصل متزايدة؛ ومنعُ عودته يُلغي الاحتفاظ. فالمطبَّق: التهدئة تُرتّب
**الحشو** (وهو مصدر التكرار المقاس)، والمراجعة المجدولة بـFSRS تعود كما صُمّمت.

**٢) «إن تجاوز أي بنك ١٥٪ فأوقف التوليد» — التوليد ليس السبب بل العلاج.** التكرار جاء من
صِغَر البنوك مقابل الاستعمال، وإيقاف التوليد يُصغّرها أكثر فيزيد التكرار. والدليل في
الجدول: `stat` (٨٩ عنصراً، بلا توليد) تكراره **٠٪**، و`gram` (٥ لكل مستوى) **٨٠٪**.

**٣) SM-2 لم تُستبدَل بـFSRS-6** — انظر جدول المعايير أعلاه.

## أربعُ صيغٍ رياضية كانت تُعرض مقلوبة — واللقطة وحدها كشفتها — ٢٥ أغسطس

الاختبارات الاثنان والثلاثون في `test_stat.js` تنجح كلّها، **ولقطة الشاشة أظهرت سطراً
يُدرّس صيغةً خاطئة**: المصدر «الانحراف الربيعي = (Q₃ − Q₁) ÷ 2 = 2.5» يُعرض
«… ÷ 2.5 = 2» — أي أن محمداً يقرأ قسمةً على ٢٫٥ لا على ٢. والنصّ في DOM سليمٌ تماماً.

| المصدر | المعروض |
|---|---|
| `(Q₃ − Q₁) ÷ 2 = 2.5` | `(Q₃ − Q₁) ÷ 2.5 = 2` |
| `Q₃ = 13 − 12 = 1` | `1 = 12 − 13 = Q₃` |
| `= 15C3 = 455` | `= 455 = 15C3` |
| `13P4 = 715 × 4!` | `!13P4 = 715 × 4` |

**والشكل المشترك**: سطرٌ عربيّ فيه رمزٌ لاتيني (Q₃، 15C3) ثم **ذيلٌ رقميٌّ صرف**. قاعدةُ
٢٥ أغسطس (بلا عربية ⇒ يسار، وفيه عربية ⇒ يمين) تُصيب السطر كلَّه ولا تُنقذ هذا الذيل.

**وكيف عُرفت الأربعة — والدرس أهمّ من العدد**: بنيتُ كاشفاً آلياً فأبلغ عن **١١١** سطراً،
فصوّرتُ ستّاً منها فإذا **خمسٌ سليمة**. فرُفض الكاشف ولم يُشحَن: **اختبارٌ بهذه النسبة من
البلاغ الكاذب لا يحرس شيئاً بل يُدرِّب على تجاهله.** ثم ضُيّقت القاعدة إلى الشكل أعلاه،
وصُوِّرت **أربع عشرة** حالة، فكانت المنقلبة **أربعاً** فقط.

**وعزلُ UAX #9 (U+2066/U+2069) جُرِّب علاجاً عامّاً فأفسد سليماً**: «13 − 3 = 10» صارت
«10 = 3 − 13»، و«0.527 × 100%» صارت «100% × 0.527». فرُفض **بالقياس لا بالرأي** — وهذا
يُفسّر لماذا فشلت محاولة العزل السابقة (٢٥ أغسطس) أيضاً. والعلاج الباقي هو المعتمَد:
**المعادلة تُفصَل في سطرٍ بلا عربية** فتُرسَم LTR. ويحرسه `tests/test_bidi_math.js`
حارسَ ارتدادٍ على الصيغ الأربع بأعيانها.

## جلسة ٢٦ أغسطس — تكرار إلياس، ووصول محتوى هيا، وقسمٌ خرج من حبسه

### السبب الحقيقي لتكرار إلياس: التوليد يعمل ولا يصل

بيانات اليوم: **عنصران مولَّدان فقط** وصلا الثلاثة جميعاً، والباقي مؤلَّف. والسجلّ يُظهر
عناصر مولَّدة بُنيت ثمّ أُسقطت توأمين («poster about AlUla» و«model of AlUla»).
وأصله في الخادم: `GEN_TOPICS[Math.random()]` لكل نداءٍ على حدة، وأربع نداءات متوازية
على ٢٩ موضوعاً تتصادم في نحو **١٩٪** من الدفعات.

**والعلاج طبقتان**: رفضُ التوأم **عند التخزين** (`genBankAdd`)، و`topicIdx` يُبذَر
متزايداً من العميل ويحترمه الخادم (`pickTopic`) — إجبارٌ بنيوي لا احتمال.

**وسببٌ ثانٍ من إصلاحي أنا**: رفعُ مفاتيح البنوك (٢٥ أغسطس) لإزالة الأسماء الغربية
**أفرغ بنوكه المولَّدة**، فبدأ اليوم من الصفر — ولهذا قال السجلّ «بقي بلا عرضٍ سابق: ٠».

### وعتبة التخزين تُشتقّ من توزيعٍ مقاس لا تُساوى بعتبة الجلسة

أوّل تنفيذٍ استعمل `GEN_TWIN` (٠٫٠٢) نفسها، وكاد يخنق البنوك. والقياس على أزواجٍ
**متمايزة يقيناً** داخل المستوى: listen/read/gram/step صفرٌ من ٤١٦ زوجاً يتجاوزها،
وvideo ٣٪، و**write ٣١٪ من ٣٠٣** لأن نصوصها تتشارك قالب الامتحان. فعتبةٌ واحدة كانت
سترفض ثلث ما يُولَّد للكتابة — أي أن الإصلاح كان سيصنع العطل الذي جاء يمنعه.
فصارت `STORE_TWIN_DEFAULT=0.15` و`STORE_TWIN_LOOSE=0.60`، فوق أرضيّة الضجيج المقاسة
لكل بنك. والتوأم الحقيقي المرصود ٠٫٣٧٥ فيُمسَك بفارقٍ واسع.

### هيا: العناصر تدخل الخطّة ولا تبلغها الجلسة

٠ من ٩ في البناء ثاني يومٍ، والتسعة كلّها A2 مستحقّة، وعناصر A1 العشرة **لم يظهر منها
واحد**. وفحصُ البانية بحالتها الحقيقية أثبت السبب: تدخل فعلاً لكن **بعد خمسة عشر عنصراً
مستحقّاً**. فالعيب ترتيبٌ لا إقصاء — i+1 سقفٌ لا أرضية. صار الترتيب داخل كل نوع بالنطاق
(مستواها ثم i+1، ثابتاً داخل النطاق فلا يُلغى تباعد FSRS). النتيجة: أوّل أربعة عناصر
بناءٍ صارت A1 بدل صفر.

### بناء الجملة قسماً مستقلّاً للأخوين

كان محبوساً في `eng_plan`، والخطّة مبنيّةٌ على منهج الصف السادس فلا تُعرَض عليهما
(`only:"coach"`). و`ENG_BUILD` ليس من المنهج. **وفائدةٌ قياسيّة**: لم يكن لبنك الكلمات
زمنٌ مرجعيّ لمنخرط (لا أحد غيرها يستعمله)، فتعذّر اشتقاق أرضيّة بوّابة تسرّع له
**بالقياس**؛ وبإخراجه للأخوين صار ممكناً. أزرار الأخوين: محمد ١٣، إلياس ١٢.

### تعارض مسطرتَي النطق يُوسَم ولا يُبتلَع

صفٌّ حقيقي: درجة ٩٣٪ مع اكتمال Azure **٢٩٪** — لأن المحاذاة تفشل بسبب الاختصارات
(`can't` كلمةٌ عنده وكلمتان عندنا) فيسقط الحساب إلى مطابقة النصّ، وهي المسطرة الضعيفة
التي وُصل Azure للخلاص منها. يُوسَم `misaligned`/`misaligned_pass` **ولا تُغيَّر الدرجة**
حتى يُقاس تكراره — عتبةٌ تُسمّى من توزيعٍ حقيقي لا تُخترع.

### وأثر ما شُحن أمس — أوّل قياسٍ حيّ

نجح: Azure على الجمل (الثمانية كلّها `engine='azure'` بحقول فونيمات، والشوا `ax` أضعف
صوتٍ في ثلاثٍ من ثمانٍ)، وبوّابة STEP (زمن هيا ٣٫٣ث ⇐ ٨٫٨ث، و٠/٦ ⇐ ٢/٦)، وبوّابة
المستوى في البناء (صفر عنصر B1)، وانحياز الموضع صار قابلاً للقياس، والتعريب وصل
(«Thanks, Hasan!»). ولم ينجح: `build1` بقي ٠ (السبب أعلاه).

**والخادم نسخة ٢٩** (٢٦ أغسطس): `topicIdx`، وMisfer بدل Musfir، والأفلاج والحمر.

## المفتوح الآن

- مسألة «دفع سالم ٪ من راتبه» صفر صواب من ثلاث. صارت مستحقّة الإعادة وبطاقة
  التكرار تنتظرها. **إن أخطأتها رابعةً فالتنبيه لا يكفي**، والعلاج التالي بطاقة
  تعليمية: «الباقي = ١٠٠٪ − المدفوع» بمثال محلول.
- **سؤال النرد ليس «صدى رقم»** — كانت تلك فرضيّتي وكذّبتها بيانات ١٣ أغسطس: ثلاث
  مرّات، ثلاثتها ±١ في ٧−العدد. وهو طرحٌ متنكّر، فعلاجه علاج الطرح.
- أخطاء المرونة حسابية لا منطقية: تكتشف فيبوناتشي ثم تُخطئ الجمع بواحد (٥+٨ ⇐ ١٢،
  و٢١+٣٤ ⇐ ٥٦). **الحكم على بوّابتَي الطرح يشمل هذه**: إن لم تهبط أخطاء ±١ في
  المرونة والكمّي فالعلاج لم ينتقل خارج قسمه.
  **وتأكّد فعلياً (١٦ أغسطس)**: يوم كامل من quant (٢٤ سؤالاً) ⇐ ٥/٢٤ = ٢١٪، أقلّ من
  التخمين العشوائي (٢٥٪ بأربعة خيارات). و٨ من ١٩ خطأً (٤٢٪) فرقها بالضبط ±١ — سؤال
  «مجموع خمسة أعداد متتالية» تحديداً ظهر أربع مرّات وأُخطئ الأربع كلّها، ثلاثٌ منها
  بفارق ١- بالضبط. **واقترحتُ ابتداءً وصل بوّابتَي `ob1`/`bt` (نفس أداة الطرح) إلى
  quant — وتراجعتُ عنه بعد فحص الشيفرة**: `bugOf('sub',mid,offset,got)` لا يطابق
  فعلياً (`count_all_not_gaps` يتنبّأ بـ+١ لا ١-، والبوّابتان مبنيّتان لعمود
  عشرات/آحاد ثنائي الرقم لا لهذا الشكل من المسائل) — إلصاق واجهة الاستلاف/القفز
  على مسألة كلاميّة لا تُطرح كـ`a-b` مباشرة كان سيكون «بناءً من الصفر» متنكّراً
  بثوب «موصول». **فشُحن نصف الحلّ الصادق**: `quizErrDelta` تُسجّل شكل الخطأ («١٧
  سم² · الصواب ٦٠ سم² · فرق −٤٣») لكل اختبارات الاختيار من متعدد (`choose()` نقطة
  واحدة تخدم quant/verbal/science/flex معاً)، بنفس عُرف `basicsErrDelta` — قياسٌ
  لا علاج، حتى تتراكم بيانات كافية لتسمية الآلية الحقيقية بدل تخمينها من قرب المسافة
  وحدها. **وسببٌ إضافي للحذر**: مولّدات quant نفسها (`gPerim`/`gMedian`/`gConsec`
  وغيرها) تُمرّر `[ans+1,ans-1,...]` كمموّهات مؤلَّفة يدوياً — فقربُ إجابتها من
  الصواب بمقدار واحد قد يعكس أقرب خيارٍ خاطئ معروض لا خطأً معرفياً حقيقياً، تماماً
  كما كذّبت بيانات الطرح فرضيّة «صدى الرقم» سابقاً. لا يُدَّعى شيء حتى يثبته القياس.
- عائلة `−١` في الطرح (أسقطت آحاد المطروح منه المنتهي بـ١، ٤ من ١٥) بلا بوّابة بعد.
- سؤالان منطقيان بالجواب نفسه («يذهبون إلى المستشفيات») — الخيارات تُسجَّل الآن،
  فالجلسة القادمة تحسم: أخللٌ في التوليد أم انحيازُ موضع؟
- **تحيّز الأذن يُقاس ولا يُدَّعى**: عدّاد `hinted` في الردّ، والحكم عليه أن تختفي
  الجمل التي يبني عليها الشريك موضوعاً لم يُطلب. **وقد يُحيّز خطأً** — احتمالٌ قائم
  ولا يُنكَر، فإن ظهر في السجلّ فالحقل يُضيَّق أو يُنزَع.
- **الشريك يسأل عن أسماء وأماكن شخصية**، ورفضها إلياس مرّتين صراحةً
  (`This is not your business`) فخرجت الجلسة عن موضوعها. لم يُنفَّذ بعد.
- إلياس: /r/ ضعُف في جملتين (٥٧ ثم ٥٢)، ومتوسّط نطقه ٩٠٪ في عشر جمل — **ودقّة
  الدرجة نفسها غير مُثبتة** حتى يُشغَّل `pron_bench`.
- الكوكب الأحمر: ثلاث مرّات، ثلاث إجابات «المشتري».
- صوت /g/ في النطق والإملاء (`gate` ⇐ `kate`).
- خطوة تصنيف الأسماء (يُعدّ/يُقاس) شُحنت ولم تُجرَّب بعد. **الحكم عليها**: درجة
  تدريبات `a few / a little` **بعد** عبور البوّابة، لا قبلها.
- `tests/pron_bench.js` لم يُشغَّل: يحتاج جهازاً متّصلاً. وحتى يُشغَّل تبقى كل
  دعوى عن دقّة درجة النطق **غير مُثبتة** — لا مكذَّبة ولا مصدَّقة.
- أثر FSRS وحَكَم LanguageTool يُقاس بعد جلسات لا بالنشر: للأوّل نسبة الصواب في
  المستحقّ عند حلوله، وللثاني عدّاد `lt:` في سجلّ المراجعة — كم بطاقةً أُسقطت.
- **الحكم على التشابك**: نسبة الصواب في الأساسيات **تهبط** في أثناء الجلسة المتشابكة
  وهذا متوقَّع ولا يُفزِع — الحكم على الاختبار المؤجَّل (موهبة الكمّي) بعد أسبوعين.
- **الحكم على معدّل الطلاقة**: خطّ الأساس يُقرأ من أوّل جلسة بعد الشحن، ثم يُتابَع.
  والنطاقات (٤٠/٢٠) **إرشادية لم تُعايَر على هيا** — تُقرأ اتجاهاً لا درجةً.
- **الحكم على مكتبة الأخطاء**: نسبة الإجابات الخاطئة التي **تُسمّى** (لا تعود «فرق فقط»).
  فإن بقي كثيرٌ بلا اسم فالفهرس ناقصٌ ويُوسَّع من الأدبيات لا من حدسي.
- **الحكم على بنك الكلمات** (وهو التزامٌ لا خيار): متوسّط طول جملة هيا في المحادثة
  ونسبة النسخ الحرفيّ — الأساس ١٤ حرفاً و٤ من ١٥. فإن لم يتحرّكا بعد جلسات فالبنك
  لم ينفع، ويُقال ذلك صراحةً بدل تزيينه. والاستعلام: `domain='coach' and qtype='say'`
  لطول الجملة، ومقابلةُ كل `say` بسطر `SAY:` قبله للنسخ.
- **السلسلة اليومية لم تُضَف عمداً**: هيا استعملت التطبيق ١٩ يوماً من ٢٢ (٨٦٪).
  لا تُضَف حتى تهبط هذه النسبة — علاجٌ لمرضٍ غير موجود يُضيف ضغطاً بلا مكسب.
